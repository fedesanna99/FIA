# v2.6.2.1 polish · Investigation T1

**Data**: 2026-05-26
**Sources finding**: `docs/v2.6.2-shell-smoke_report.md` (post smoke E2E live)

## F1 · ViewportHud legacy in codepath useNewShell

### Callsite (grep `ViewportHud|ViewportCanvasTabs`)

| File | Cosa | Ruolo |
|------|------|-------|
| `frontend/src/components/viewport/ViewportHud.tsx` | def componente legacy | sorgente — chip flex-wrap floating top-3.5 |
| `frontend/src/components/viewport/Viewport3D.tsx:29` | import | viene importato |
| `frontend/src/components/viewport/Viewport3D.tsx:169` | `<ViewportHud />` | **sempre montato** dentro Viewport3D, indipendentemente da useNewShell |
| `frontend/src/components/shell/ViewportCanvasTabs.tsx` | def tabs legacy | sorgente — 5 tab "Modello/Carichi/Mesh/Risultati/Checks" |
| `frontend/src/App.tsx:42` | import | |
| `frontend/src/App.tsx:535` | `<ViewportCanvasTabs />` | **renderizzato dentro `<Shell>{children}`** quando useNewShell=true |
| `frontend/src/App.tsx:577` | `<ViewportCanvasTabs />` | renderizzato nel codepath legacy (corretto) |

### Diagnosi

In `App.tsx:531-545` quando `useNewShell=true`:
```tsx
<Shell>
  <div className="absolute top-0 left-0 right-0 z-10">
    <ViewportCanvasTabs nodes={...} elements={...} />   // <-- LEGACY tabs
  </div>
  <div className="absolute inset-0 pt-[36px]">
    <Viewport3D />                                       // <-- include <ViewportHud /> al line 169
  </div>
  <DropZone />
  <SolverOverlay />
</Shell>
```

Quindi:
- `ViewportCanvasTabs` è montato esplicitamente in App.tsx, anche dentro nuova Shell
- `ViewportHud` è montato implicitamente da Viewport3D.tsx, sempre

### Fix scelto

**App.tsx**: rimuovi `<ViewportCanvasTabs />` dal codepath useNewShell.
La nuova Shell ha già i 5 tab in `ShellPanel` come Radix Tabs — i 5 tab vecchi sopra il viewport sono duplicati ridondanti.

**Viewport3D.tsx**: aggiungo prop `suppressHud?: boolean` (default false per preservare chrome legacy). App.tsx passa `suppressHud={true}` quando useNewShell. Render guard: `{!suppressHud && <ViewportHud />}`.

**Niente nuovo hook `useNewShell()`**: la condizione è calcolata in App.tsx e propagata via prop. Single source of truth.

## F2 · ShellCommandPalette non legge paletteItems registry

### Inventario

- `frontend/src/lib/paletteItems.ts` (421 righe): definizione `PaletteItem[]` (~140 voci), esporta `PALETTE_ITEMS`, `SECTION_LABELS`, `SECTION_ORDER`, `PALETTE_COUNT`
- `frontend/src/components/shell/CommandPalette.tsx` (555 righe): legacy palette + dispatcher `execute(item)` (~250 righe di switch su `actionKind`)
- `frontend/src/shell/ShellCommandPalette.tsx` (v2.6.2 attuale): 7 voci hardcoded, non legge registry

### actionKind del registry (17 tipi)

`workspace | right-panel | tools-view | dialog | theme | run-analysis | external-link | togglePalette | openHelp | openAccount | openLocation | openAuth | openExport | logout | focus-toggle | open-import-wizard | open-wizard | open-template-gallery | apply-material | apply-section | toggle-view | view-preset | quick-export | goto-node | goto-element`

### Fix scelto

Estraggo il dispatcher in nuovo hook `frontend/src/lib/usePaletteDispatch.ts`:
- Replica logica `execute(item)` del legacy
- Importato sia da `CommandPalette.tsx` (legacy) sia da `ShellCommandPalette.tsx` (nuovo)
- Single source of truth per dispatcher

Refactor `ShellCommandPalette.tsx`:
- Importa `PALETTE_ITEMS` da `lib/paletteItems`
- Renderizza Command.Item per ogni voce + uso `usePaletteDispatch` per onSelect
- Grouping per `section` (commands/panels/settings/loads/help)
- Preserva preconditions (`needsModel`, `soon`)

Refactor `CommandPalette.tsx` legacy:
- Sostituisco il body di `execute()` con chiamata a `usePaletteDispatch`
- Tutto il resto invariato

## F3 · tb-model-id slug semantico

### Stato attuale

`frontend/src/shell/ShellTopBar.tsx:67`:
```tsx
{model?.id && <span className="tb-model-id">{model.id.slice(0, 6).toUpperCase()}</span>}
```

UUID modello tipo `EX_PORTALE_2D_v1` → "EX_POR" (primi 6 char maiuscoli).

### Slug semantico atteso

Mockup mostrava "UC1" (Use Case 1 short id). Non abbiamo questa metadata
nel modello FEA, ma possiamo derivarla dal **nome modello** prendendo
le iniziali maiuscole.

```ts
"Telaio portale 2D" → ["Telaio", "portale", "2D"] → "TP2"
"Mensola lineare" → ["Mensola", "lineare"] → "ML"
```

### Inventario tipi

`frontend/src/types/model.ts` (grep `shortId|slug`): nessun campo dedicato.
Quindi soluzione derivata da name.

### Fix scelto

Helper `modelShortId(model)` inline in `ShellTopBar.tsx`:
- Prende prime lettere uppercase di ogni parola del nome
- Limit 4 char
- Fallback su UUID se name vuoto

## Tabella out-of-scope

| Cosa | In/Out |
|------|--------|
| Refactor `paletteItems.ts` registry | OUT (solo lettura) |
| Modifica logica preconditions | OUT |
| Rimozione completa `ViewportHud.tsx` | OUT (chrome legacy lo usa) |
| Migration `goto-node`/`goto-element` dinamici nella nuova palette | OUT (resta nel legacy fino a Fase 5) |
| Toast feedback differenziato palette nuova/legacy | OUT |
