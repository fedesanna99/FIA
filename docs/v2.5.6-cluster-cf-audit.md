# Cluster C+F · audit precondizioni e pannelli

**Data**: 2026-05-26
**Scope**: T1 brief v2.5.6 — mappare pannelli, precondizioni, empty state, prima di implementare registry + FeatureButton + migrare callsite.

## 1. Asset esistenti che riusiamo (no reinventare)

| Asset | File | Note |
|-------|------|------|
| `Button` primitive | `frontend/src/components/ui/Button.tsx` | Già ha `variant`, `size`, `loading`, `iconLeft/Right`, accetta tutte le `ButtonHTMLAttributes`. `FeatureButton` wrappa `Button` invece di reinventare lo stile. |
| `Tooltip` | `frontend/src/components/ui/Tooltip.tsx` | Radix wrapper con `content: ReactNode`, `side`, `align`, `disabled`. Pattern già documentato. |
| `EmptyState` | `frontend/src/components/ui/EmptyState.tsx` | Esiste già con `title`, `description`, `icon`, `action: ReactNode`, `className`. Sufficiente per T6 (no nuovo componente). |
| `useModelStore` | `frontend/src/store/modelStore.ts` | `selectedNodeIds: Set<number>`, `selectedElementIds: Set<number>` (NB: `number`, non `string` come da brief). |
| `useModelHistory` | `frontend/src/store/historyStore.ts` | `past[]`, `future[]`, `canUndo()`, `canRedo()`. |
| `useResultsStore` | `frontend/src/store/resultsStore.ts` | `staticResults`, `modalResults`, `dynamicResults` (interface `ResultsState` privata, non esportata). |
| `useWorkspaceStore` | `frontend/src/store/workspaceStore.ts` | `openLeftPanel`, `openRightPanel`, `currentLeftPanel`, `currentRightPanel`. **Già supporta mutex implicito**: setter aggiorna solo il proprio rail (left/right indipendenti, no cross-conflict). |

## 2. Pannelli principali (cluster C)

| Pannello | File | Rail | Stato store |
|----------|------|------|-------------|
| Make | `frontend/src/shell/panels/MakePanel.tsx` | left | `currentLeftPanel === "make"` |
| Solve | `frontend/src/shell/panels/SolvePanel.tsx` | left | `currentLeftPanel === "solve"` |
| Verify | `frontend/src/shell/panels/VerifyPanel.tsx` | left | `currentLeftPanel === "verify"` |
| Inspect | `frontend/src/shell/panels/InspectPanel.tsx` | right | `currentRightPanel === "inspect"` |
| View | (rail right) | right | `currentRightPanel === "view"` |
| Tools | (rail right) | right | `currentRightPanel === "tools"` |
| History | (rail right) | right | `currentRightPanel === "history"` |

**Verdetto cluster C panel mutex**:
- Mutex **intra-rail** (es. Make vs Solve sullo stesso left rail): **già funzionante** — `openLeftPanel(id)` setta `currentLeftPanel: id` che è single-valued. Aprire Solve sostituisce Make.
- Mutex **cross-rail** (left + right insieme): **per design coesistono**, è dual-pane intentional (es. Solve aperto + Inspect aperto a destra).
- **BUG-014** ("View attivata non visibile"): plausibilmente regression di overlay viewport rendering (deformata 3D), NON un problema di workspaceStore. **Out of scope T6**, segnalato come finding per S3 cluster A (viewport).
- **BUG-064** (pannelli conflitto): se intra-rail, già coperto dal setter; se cross-rail intentional, non è bug. **Closure parziale tramite documentazione**.

## 3. Feature con precondizioni (cluster F)

Lista enumerata in `lib/preconditions.ts` (T2):

| FeatureId | Precondizioni | Callsite UI atteso | disabledAction propositiva |
|-----------|---------------|---------------------|------------------------------|
| `run-static` | model + constraints + loads | `SolvePanel.tsx:161` bottone "Esegui" | none |
| `run-modal` | model + constraints | `SolvePanel.tsx` (in `AnalysisRow modal`) | none |
| `run-buckling` | model + constraints + loads | idem (`AnalysisRow buckling`) | none |
| `verify-ec3` | static-results | `VerifyPanel` tab EC3 | `run-static` |
| `verify-ec2` | static-results | `VerifyPanel` tab EC2 | `run-static` |
| `verify-ec8` | static-results | `VerifyPanel` tab EC8 | `run-static` |
| `export-pdf` | model | `ExportView.doPdf`, `AvatarMenu`, palette `exp-pdf-full` | none |
| `export-xlsx` | model | `ExportView.doExcel`, palette `exp-xlsx` | none |
| `inspect-node` | node-selected | rail Inspect view "nodo" | none |
| `inspect-element` | element-selected | rail Inspect view "elemento" | none |
| `undo` | history-can-undo | `TopBar.tsx:78` bottone undo | none |
| `redo` | history-can-redo | `TopBar.tsx:79` bottone redo | none |

## 4. Pattern `disabled={!model || ...}` esistenti (scoperti via grep)

Solo i 5 callsite principali sono target T5. Gli altri (~25 callsite) restano per debt sprint:

### Target T5 (5 migrazioni)

| # | File:riga | Pattern attuale | featureId target | propose-action |
|---|-----------|-----------------|-------------------|-----------------|
| 1 | `SolvePanel.tsx:161-175` | `disabled={!model \|\| isRunning}` | `run-static`/`run-modal`/`run-buckling` (dinamico via `selectedLinear`) | no (loading state già gestito) |
| 2 | `TopBar.tsx:~78` undo | `canUndo = useModelHistory((s) => s.past.length > 1)` poi `disabled={!canUndo}` | `undo` | no |
| 3 | `TopBar.tsx:~79` redo | `canRedo = useModelHistory((s) => s.future.length > 0)` poi `disabled={!canRedo}` | `redo` | no |
| 4 | `VerifyPanel.tsx` | EmptyState in caso `!staticResults` (da implementare con `<EmptyState>` esistente + `<FeatureButton featureId="run-static">` come action) | n/a — uso EmptyState | n/a |
| 5 | `ExportView.tsx` doPdf/doExcel | `disabled` non esplicito ma early return su `!model` | `export-pdf`/`export-xlsx` | no |

### Out of scope T5 (~25 callsite, debt sprint)

Tutti i pattern `disabled={!model}` o `disabled={!staticResults}` o `disabled={isRunning}` nei panel/dialog rimanenti (es. `NonlinearPanel`, `ArcLengthPanel`, `SismicaTHWizard`, etc.). Documentati come "debt" da migrare opportunisticamente o in sprint dedicato.

## 5. Empty state esistenti vs target (cluster F BUG-058/061/062)

| Pannello | Empty state oggi | Target post-T6 |
|----------|-------------------|-----------------|
| Verify (no risultati statici) | hub mostra tab vuote o errore al run | `<EmptyState title="Nessun risultato da verificare" description="..." action={<FeatureButton featureId="run-static" proposeActionOnDisabledClick onProposeAction={runStatic}>Esegui analisi statica</FeatureButton>} />` |
| Inspect (no selezione) | NodeDetail/ElementDetail vuoti | `<EmptyState title="Nessun elemento selezionato" description="Seleziona un nodo o un elemento dal viewport per ispezionarne le proprietà." />` |
| Results (no analisi eseguita) | banner generico | `<EmptyState ... action={<FeatureButton featureId="run-static">Esegui analisi</FeatureButton>} />` |

**Decisione T6**: faccio Verify e Inspect (2 callsite ad alto impatto), Results lo lascio per debt sprint se è già funzionante con altre forme.

## 6. STOP rules verificate

- **STOP #1** (lib/preconditions.ts esiste): NO, non esiste. Procedo a crearlo.
- **STOP #2** (FEAModel forma diversa): la forma è chiara — `constraints[]`, `loads[]`, `nodes[]`, `elements[]` (vedi `types/model.ts:77-95`). Adatto selettori.
- **STOP #3** (Tooltip pattern): Radix tooltip già wrappato in `Tooltip.tsx`. Riuso.
- **STOP #4** (loading state più complesso): `FeatureButton` estende interface per `loading` prop pass-through al `Button` primitive.
- **STOP #5** (panel mutex già funziona): YES, intra-rail mutex già OK. T6 panel mutex specifico ridotto a documentazione + EmptyState callsite.
- **STOP #6** (vitest scende): da verificare a fine sprint.
- **STOP #7** (build cresce >20%): registry + FeatureButton sono <5kB combinati, no rischio.

## 7. Scope finale confermato

- **T2**: `lib/preconditions.ts` nuovo (registry 12 feature, 9 precondizioni, `checkFeature`, `evaluatePrecondition`)
- **T3**: `components/ui/FeatureButton.tsx` (wrapper `Button` + `Tooltip`)
- **T4**: `hooks/usePreconditions.ts` (compone snapshot da store)
- **T5**: 4 migrazioni (SolvePanel run + TopBar undo/redo + ExportView/AvatarMenu export). NB: i 3 verify button (EC3/EC2/EC8) li migro inline negli EmptyState di T6 invece che separati.
- **T6**: `EmptyState` callsite Verify + Inspect con CTA che usa FeatureButton + propose-action
- **T7**: report
