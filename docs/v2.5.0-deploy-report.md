# v2.5.0-paolo-ready · Deploy report

**Data**: 2026-05-26
**Target**: https://fea-pro.fly.dev/
**Commit deployato**: `282336e` (tag `v2.5.0-paolo-ready`) — confermato via marker HTML nel bundle servito
**Commit HEAD locale**: `125077d` (post smoke E2E commit, discende da 282336e)

## Esito

✅ **PASS** — deploy completato, machine started, health check 200, SPA carica con marker v2.5.0.

## Step eseguiti

| # | Step | Esito | Note |
|---|------|-------|------|
| Pre-flight | `fly auth whoami` | ✓ | logged as `fedesanna99@gmail.com` (warning context canceled non bloccante) |
| Pre-flight | git status pulito + HEAD discende da 282336e | ✓ | |
| T1 | Dockerfile + fly.toml verificati | ✓ | `app = "fea-pro"` |
| T2 | `fly deploy --remote-only` | ✓ | image push + rolling update ok |
| T3 | `fly status` | ✓ | release 85 attiva, 1/1 check passing |
| T3 | `curl /api/health` | ✓ | HTTP 200, `{"status":"ok","version":"1.0.0"}` |
| T3 | `curl /` (SPA) | ✓ | HTTP 200, 1655 bytes, `<title>FEA Pro — Analisi Strutturale</title>` |
| T4 | Endpoint version discovery | ⏭ N/A | `/api/version` e `/api/info` ritornano 404. `/api/health` espone solo `version: "1.0.0"` (hardcoded FastAPI). Conferma versione via marker HTML (vedi sotto). |

## Conferma versione deployata (cruciale)

L'HTML servito da `https://fea-pro.fly.dev/` contiene il commento:

```html
<!-- v2.5.0 PR1 destructive fix: aggiunto Inter Tight (display), rimosso IBM Plex Sans (legacy non usato in Precision). -->
```

Questo marker è stato introdotto in `frontend/index.html` durante la merge di PR1 Precision (commit `c32005e` del 24/05/2026), che è ancestor di `282336e`. La sua presenza nel bundle servito **conferma** che il deploy attuale serve codice ≥ v2.5.0-paolo-ready, NON più v2.3.x come prima del deploy.

Bundle JS principale: `index-BV5YDnOA.js` — coincide con il build artifact log del `fly deploy` di questo turn (vedi `/tmp/fly-deploy.log` Stage 1 Vite output).

## Output chiave

| Campo | Pre-deploy | Post-deploy |
|-------|------------|-------------|
| Release | v84 | **v85** |
| Image tag | `deployment-01KSDH9X8MY39Y0XZ0RYYY3PZM` | **`deployment-01KSHQ1XQ28BG31R32EA47M1EA`** |
| Image SHA | (prev) | `sha256:c989e9f6...51410952723` |
| Image size | n/a | **387 MB** |
| Machine ID | `d8d2949c042208` | `d8d2949c042208` (stessa machine, ruotata) |
| State | started | **started** |
| Checks | 1/1 passing | **1/1 passing** |
| Region | `fra` | `fra` |
| Bundle JS | (vecchio) | **`index-BV5YDnOA.js`** |

## Tempi

- **Fronte build Vite (Stage 1)**: 32.5s ("✓ built in 32.52s")
- **Stage 1 totale (Docker)**: 57.5s
- **Push image registry.fly.io**: ~1s (layer cache hit per molti layer)
- **Rolling update machine**: ~10s (acquire lease → update config → reach started)
- **Tempo totale `fly deploy` (wall clock)**: ~3 minuti effettivi (molto sotto gli 8-12 min stimati grazie alla cache di layer Python invariati)

## Warning gestiti (non bloccanti)

1. **`Metrics token unavailable: failed to run query... context canceled`** — warning ricorrente del CLI `flyctl` su tutti i comandi. Documentato già in `docs/v2.5.0-pre-deploy-lookup.md` § "Rischi". Non bloccante: tutti i comandi funzionali (deploy, status, logs) hanno successo. Probabile background query a un endpoint metrics organization scaduto/non disponibile, non impatta la sessione auth.

2. **`The app is not listening on the expected address`** — emesso da Fly subito dopo l'update della machine config, MA seguito immediatamente da `✔ Machine d8d2949c042208 is now in a good state` + health check Fly passing. Race timing: il warning viene generato prima che uvicorn finisca lo startup (~5-8s), poi il check `/api/health` del fly.toml (interval 30s, grace 10s) verifica e dichiara passing. Conferma: il `curl /api/health` post-deploy ritorna HTTP 200.

3. **`Chunks larger than 700 kB`** — warning Vite atteso, già storico (chunk `three.js` 858kB, `index` 1277kB). Non blocca, non impatta UX.

## Note

- Il warning "not listening" + race con healthcheck è un pattern noto Fly su app uvicorn con startup lento. Non richiede fix nel codice.
- La machine ID `d8d2949c042208` è la stessa pre e post (rolling deploy single-machine). Il volume `fea_data` resta correttamente montato (i modelli/jobs/audit non sono persi).
- Nessun rollback richiesto. STOP rule #3 non attivata.

## Prossimo passo

Re-run smoke E2E live: `cd frontend && FEAPRO_TEST_EMAIL=... FEAPRO_TEST_PASSWORD=... npx playwright test paolo-smoke-live.spec.ts --config=playwright.smoke-live.config.ts --reporter=list`. Atteso PASS di STEP 0-1 (già passavano pre-deploy), nuovo PASS atteso STEP 2-4 (i `data-feature-id` ora esistono nel DOM). Potrebbe ancora fallire per **Finding #2** dello smoke precedente (onboarding tour 10-step modal blocca selettori al primo login) — in quel caso patch ~3 righe nel test spec (`page.getByRole("button", { name: /^salta$/i }).click().catch(() => {})` post-login).

Brief separato raccomandato: `v2.5.0-paolo-ready-smoke-e2e-live-rerun`.
