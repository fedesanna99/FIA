# v2.6.2 Shell · Blueprint dal mockup

**Data**: 2026-05-26
**Sources**:
- `design-handoff/ui_kits/webapp_desktop/Nuovo Guscio.html` (54 righe, scheletro)
- `design-handoff/ui_kits/webapp_desktop/nuovo-guscio.css` (992 righe, stili completi)
- `design-handoff/ui_kits/webapp_desktop/nuovo-guscio-shell.jsx` (TopBar + Rail + Viewport + StatusBar JSX)
- `design-handoff/ui_kits/webapp_desktop/nuovo-guscio-panel.jsx` (RightPanel + workspace content)
- `design-handoff/ui_kits/webapp_desktop/nuovo-guscio-overlays.jsx` (Command Palette)
- `design-handoff/ui_kits/webapp_desktop/nuovo-guscio-app.jsx` (App root + Tweaks integration)
- `design-handoff/DESIGN_HANDOFF.md` §3.0..3.6

## Layout grid

- Grid template rows: `48px / 1fr / 28px` (TopBar / Mid / StatusBar)
- Grid template cols Mid: `56px / 1fr / 380px` (Rail / Viewport / RightPanel)
- CSS vars: `--topbar-h`, `--statusbar-h`, `--rail-w`, `--panel-w`

```css
.shell {
  display: grid;
  grid-template-rows: var(--topbar-h) 1fr var(--statusbar-h);
}
.shell-mid {
  display: grid;
  grid-template-columns: var(--rail-w) 1fr var(--panel-w);
}
```

## TopBar (h-48px)

| Elemento | Specifica esatta dal mockup |
|---|---|
| Brand block | mark 24px cyan (`F`) + "FEA Pro" 14px/700 + tier badge ("FREE" mono uppercase 9px) + version ("v2.6") mono 10px |
| Model selector (`.tb-model`) | h-30, padding 10, font 13/600. Contiene `.tb-model-id` mono (es. "UC1") + nome modello + chevron down |
| Save chip (`.tb-save`) | bg-success + border, mono 10/600 uppercase, "Salvato HH:MM" + icon check |
| Trust badge (`.tb-trust`) | bg-warn + border warn, mono 10/700 uppercase, "Preliminary" + pulse dot 6px |
| Spacer | `.tb-spacer` flex-1 |
| ⌘K search pill (`.tb-search`) | h-32, bg-hover + border, min-width 280, "Cerca azioni, modelli, norme…" + kbd `⌘ K` |
| Run button (`.tb-run`) | h-32, gradient verde #34D399→#059669, "▶ Esegui" + kbd F5 |
| Icon buttons (`.tb-iconbtn`) | 32×32, transparent + hover bg-hover. Undo, Redo, Notifications (con `.tb-iconbtn-badge` dot accent), Avatar (`FS`) 30×30 |

## Rail (w-56px)

5 workspace + separator + Auto-detect + Docs + spacer + Settings (in fondo).

Ogni rail-btn:
- 44×44, radius 10 (soft) / 0 (sharp), grid place-items center
- Hover: bg-hover + ink
- Active: bg accent-subtle + color accent + strip 3px left side (`-7px`)
- `.rail-kbd` numero in basso-destra mono 8/600 ink-faint
- `.rail-tooltip` slide-in da destra (`left: calc(100% + 10px)`) mono 11

```js
WORKSPACES = [
  { id: "modello",   icon: "Box",         kbd: "1" },
  { id: "analisi",   icon: "Cog",         kbd: "2" },
  { id: "risultati", icon: "Activity",    kbd: "3" },
  { id: "verifiche", icon: "CheckCircle", kbd: "4" },
  { id: "io",        icon: "Shuffle",     kbd: "5" },
];
// Separator + Bug (F3) + BookOpen (?) + Spacer + Settings
```

Mapping ai workspace v2.5.x esistenti:
- modello → MakePanel (Modello)
- analisi → SolvePanel (Solve, ribattezzato Analisi UI)
- risultati → InspectPanel (Risultati)
- verifiche → VerifyPanel (Checks)
- io → ToolsPanel (I/O & Collab)

## Viewport HUD (6 floating)

Tutti `.vp-hud` con bg-panel + border + radius 10 + shadow 0 8px 24px:

| HUD | Posizione | Contenuto |
|---|---|---|
| `.vp-legend` | top 16, left 16, width 220 | Eyebrow "σ VON MISES" + slider btn + colormap bar gradient + axis ticks numerici + unit "MPa · fyk · UR_σ" |
| `.vp-controls` | top 16, right 16 | Persp/Ortho + sep + Solid/Wireframe + Grid toggle (vp-ctrl-btn h-28, on=accent-subtle) |
| `.vp-selection` | top 16, center | EL tag accent + meta breadcrumb (Beam · IPE · S355 · N₅→N₆) + X close |
| `.vp-gizmo` | bottom 16, right 16, 88×88 | SVG XYZ axes (Z=blue, X=red, Y=green) |
| `.vp-ruler` | bottom 16, left 16 | "1 m" + bar 100px con tick endcap + "Zoom 100%" |
| `.vp-zoom` | bottom 16, center | Pan + sep + Zoom out + value 100% + Zoom in + sep + Fit + sep + Maximize (all transparent btn) |

Wrappers attivi su Canvas esistente:
- `ViewportHudGizmo` riusa il GizmoHelper di drei (già in `Viewport3D`)
- `ViewportHudRuler` riusa `ScaleIndicator` v2.5.7 dinamico
- Gli altri 4 HUD sono nuovi componenti UI puri (no logica viewport)

## RightPanel (w-380px)

Layout:
```
.shell-panel (flex column, overflow hidden)
├─ .sp-head (padding 14/16, border-bottom)
│  ├─ .sp-head-row (icon 28 accent-subtle + h2 16/700 + .sp-help "?")
│  └─ .sp-desc (12/17 ink-muted)
├─ .sp-tabs (padding 0 12, border-bottom)
│  └─ .sp-tab (10/12, ink-dim, active=ink + ::after accent 2px underline)
└─ .sp-body (flex-1, overflow-y auto)
   └─ .sp-section (padding 14/16, border-bottom)
      ├─ .sp-section-head (eyebrow mono 10/700 ws14 uppercase + action btn)
      └─ content (metrics, display-list, slider, ur-strip, inspector-card, snap-row)
```

Workspace contenuti:
- **risultati**: 6 sezioni (Snapshot · Risultati globali · Visualizza · Scala deformazione · Verifica EC3 · Ispettore + footer Snapshot+PDF)
- **modello**: 3 sezioni (Albero · Mesh wizard · Schema avanzato)
- **analisi**: 2 sezioni (Tipo analisi · Parametri)
- **verifiche**: 2 sezioni (Normative · EC3 UR per elemento)
- **io**: 3 sezioni (Import · Export · Strumenti avanzati)

In Fase 2 (questa): scaffolding ShellPanel + tabs Radix funzionante. Content workspace = passthrough ai componenti esistenti (MakePanel/SolvePanel/VerifyPanel/InspectPanel/ToolsPanel) finché Fase 3+ non li ristyla.

## StatusBar (h-28px)

`.shell-statusbar` flex, padding 0 14, gap 14, font mono 10/ink-dim:
- `.sb-item` connessione (`.sb-dot` success/warn/danger + "Online · fea-pro.fly.dev")
- sep + Selezione (`EL 5`)
- sep + Unità (`SI · kN, m, MPa`)
- sep + Snap (`0.10 m`)
- spacer
- Solver (`Static · 0.84s`) + sep + Collab (`Solo`) + sep + Zoom (`100%`) + sep + Counters (`N nodi · E elementi · BCs · loads`)

In Fase 2: render placeholder + dati reali da `useAnalysisStore` + `useModelStore`.

## Command palette (cmdk)

`.cmd-backdrop` (rgba 40% blackout) + `.cmd-modal` (680×90vw, radius 14, shadow forte):
- `.cmd-search` input 16/500 + icon Search + kbd "esc"
- `.cmd-list` max-h 400 con `.cmd-group-head` (mono 9/700 ws14 uppercase) e `.cmd-item` (grid 24/1fr/auto, icon 24×24, name 13/600, sub mono 10 ink-dim, kbd opzionale)
- `.cmd-foot` mono 10 con kbd hints + AI sparkles a destra

Voci raggruppate:
1. **Suggerito ora**: AI ask + Export PDF
2. **Workspace**: 5 (modello/analisi/risultati/verifiche/io)
3. **Analisi**: run-static / run-modal / run-buckling / run-pushover
4. **Verifiche**: EC3 / EC2 / EC8
5. **Modello**: mesh-line / mesh-tri / mesh-param / edit-load
6. **Sistema**: undo / docs / detect

Toggle `Ctrl+K` / `Cmd+K`. Esc chiude. Frecce naviga. Invio esegue.

In Fase 2: rendering UI nuovo via cmdk + Radix Dialog. **Registry esistente** (`paletteItems.ts`) intatto: solo skin nuovo.

## Tokens utilizzati (sample)

- Colori: `var(--bg)`, `var(--bg-panel)`, `var(--bg-hover)`, `var(--bg-success)`, `var(--bg-warn)`, `var(--bg-info)`, `var(--bg-danger)`, `var(--bg-active)`, `var(--bg-elevated)`, `var(--ink)`, `var(--ink-muted)`, `var(--ink-dim)`, `var(--ink-faint)`, `var(--border)`, `var(--border-light)`, `var(--accent)`, `var(--accent-hover)`, `var(--accent-subtle)`, `var(--success)`, `var(--warn)`, `var(--coral)`, `var(--danger)`
- Fonts: `var(--font-sans)` (Inter body), Plus Jakarta Sans display via `font-family: "Plus Jakarta Sans"` hardcoded, `var(--font-mono)` (JetBrains Mono per badge/kbd/numeric)
- Radius: 4/6/8/10/12/14 px (Soft) — override `:root[data-radius="sharp"]` = 0
- Shadow: `var(--shadow-card)`, `var(--shadow-pop)`, `var(--shadow-elev)`, `var(--shadow-dialog)`

## Files da creare in `frontend/src/shell/`

- `Shell.tsx` — orchestrator + layout grid
- `ShellTopBar.tsx` — TopBar 48px completa (8 elementi)
- `ShellRail.tsx` — Left rail 56px (5 ws + separator + 2 secondary + spacer + settings)
- `ShellViewport.tsx` — wrapper Canvas + 6 HUD
- `ShellPanel.tsx` — Right panel 380px + tabs Radix + section helper
- `ShellStatusBar.tsx` — Status bar 28px
- `ShellCommandPalette.tsx` — cmdk + Radix Dialog
- `hud/ViewportHudLegend.tsx`
- `hud/ViewportHudControls.tsx`
- `hud/ViewportHudSelection.tsx`
- `hud/ViewportHudGizmo.tsx` (wrapper)
- `hud/ViewportHudRuler.tsx` (wrapper ScaleIndicator)
- `hud/ViewportHudZoom.tsx`

CSS asset:
- `frontend/src/styles/shell.css` — copia adattata da `nuovo-guscio.css`

## Backward compatibility decisioni Fase 2

- ✅ `Button` Precision invariato (50+ callsite, migration Fase 5)
- ✅ Inter Tight font import preservato (rimozione Fase 3+)
- ✅ Tutti i workspace content esistenti (Make/Solve/Verify/Inspect/Tools)
  ospitati dentro `ShellPanel` come children, NO refactor del loro interno
- ✅ Canvas R3F **wrappato non toccato**: `<ShellViewport><Viewport3D /></ShellViewport>` pattern
- ✅ Registry palette `paletteItems.ts` intatto, solo UI rendering nuovo
- ⚠️ APP_VERSION bump da `"v2.3"` a `"v2.6"` (single source `frontend/src/lib/version.ts`)
- ⚠️ Layout root in App.tsx sostituito da `<Shell>` (operazione invasiva, vedi T9)

## Out of scope Fase 2 (rimandato Fase 3+)

- Refactor workspace content interni (sezioni metric/inspector/UR-strip del mockup)
- HelpSheet completa (T2 placeholder)
- TrustBadge logica reale (preliminary/draft/validated state)
- Migration `Button` → `Button2` (50+ callsite)
- Rimozione Inter Tight font
- ScaleIndicator restyled completo
- Dark mode UI toggle (useTheme wired ma nessun selector in topbar avatar menu)
