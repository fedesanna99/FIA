# v2.6.2 Shell · Smoke verify documentale

**Data**: 2026-05-26
**Tipo**: smoke documentale (lettura codice + esecuzione tsc + vitest + build)
**No smoke E2E live**: per rispetto del brief ("NO smoke E2E live, lo facciamo dopo Fase 2 in un brief separato")

## Visual checklist (verificato via lettura codice + classi CSS)

- [x] **TopBar h-48px** con brand + model selector + save + trust + ⌘K + run + undo/redo/notif/avatar
  - `ShellTopBar.tsx` riproduce 1:1 il mockup `Nuovo Guscio.html` linee 17-66
  - Brand: `.tb-mark` (cyan square 24px "F") + name + tier badge + version (`v2.6`)
  - Model selector: `.tb-model` con chevron-down lucide
  - Save chip: `.tb-save` verde con icon Check lucide + timestamp `lastSavedAt`
  - Trust badge: `.tb-trust` arancio + pulse dot 6px (via `@keyframes tb-trust-pulse` in shell.css)
  - Search pill: `.tb-search` apre palette via `useWorkspaceStore.setPalette(true)`
  - Run button: `.tb-run` gradient verde `linear-gradient(180deg, #34D399, #059669)`, disabilitato quando `isRunning || !model`
  - 3 icon buttons (`.tb-iconbtn`) + avatar (`.tb-avatar` 30×30 mono "FS")

- [x] **APP_VERSION mostrata: v2.6** (non più v2.3)
  - `frontend/src/lib/version.ts:8` → `APP_VERSION = "v2.6"`, `APP_TAG = "v2.6.2-shell"`
  - Renderizzato in `.tb-ver` (TopBar) e `.sb-item .sb-k` (StatusBar)

- [x] **Rail 56px** con 5 workspace + auto-detect + docs + settings
  - `ShellRail.tsx` con 5 entries: Modello (Box), Analisi (Cog), Risultati (Activity), Verifiche (CheckCircle), I/O (Shuffle)
  - `.rail-sep` separator + 2 secondary (Bug F3, BookOpen ?)
  - `.rail-spacer` flex-1 + Settings in fondo
  - Active state: `bg-active-subtle` + `color-accent` + strip `::before` 3px

- [x] **Viewport con 6 HUD posizionati senza overlap**
  - `ShellViewport.tsx` orchestra 6 HUD floating:
    - `vp-legend` top 16 left 16 width 220 (gradient colormap)
    - `vp-controls` top 16 right 16 (Persp/Ortho/Solid/Wire/Grid)
    - `vp-selection` top 16 center (mostrato solo se selezione attiva)
    - `vp-gizmo` bottom 16 right 16 (88×88 SVG XYZ)
    - `vp-ruler` bottom 16 left 16 (scale dinamica da `viewportCameraStore`)
    - `vp-zoom` bottom 16 center (Pan/ZoomOut/Val/ZoomIn/Fit/Maximize)
  - Z-index ordering via classi `.vp-hud` in shell.css

- [x] **Card overlap top-left risolto**
  - Il vecchio `ViewportHud.tsx` (chip "Telaio portale 2D" che copriva il viewport) NON è renderato nella nuova Shell
  - Info modello (nome, stats, materiale) ora vivono in: TopBar `.tb-model` (nome) + StatusBar `.sb-item` (counters) + ShellPanel header (workspace context)
  - Issue v2.5.0-paolo-ready P2 → CHIUSO architettonicamente

- [x] **RightPanel 380px** con header + tabs + sections
  - `ShellPanel.tsx` larghezza 380 fissa via CSS var `--panel-w: 380px`
  - `.sp-head` con icon + h2 + sp-help
  - `.sp-tabs` Radix Tabs.Root con tabs configurabili per workspace
  - `.sp-body` scrollabile ospita `MakePanel`/`SolvePanel`/`VerifyPanel`/`InspectPanel`/`ToolsPanel` esistenti (passthrough)

- [x] **StatusBar h-28px** con counters + mode + versione
  - `ShellStatusBar.tsx` h via `--statusbar-h: 28px`
  - Connection + Unità + Snap | spacer | Solver + Zoom + Counters + Versione
  - Reattivo a `useModelStore` (counters) + `useAnalysisStore` (isRunning)

- [x] **Command palette ⌘K apre cmdk** con voci raggruppate
  - `ShellCommandPalette.tsx` via cmdk + Radix Dialog
  - Toggle handler globale Ctrl+K / Cmd+K
  - Voci: Suggerito ora (AI ask + Export PDF) + Workspace + Analisi + Sistema
  - Esc chiude (Radix Dialog `onOpenChange`)

- [x] **Run button verde gradient** (preserva logica esistente)
  - `ShellTopBar` → `useRunAnalysis` (hook esistente) chiamato in `handleRun`
  - data-feature-id="run-static" (consistency con BUG-027 fix v2.5.6)
  - disabled quando `isRunning || !model`

- [x] **Undo/redo button** (preserva FeatureButton + preconditions v2.5.6)
  - `useModelStore.undo/redo` invocati direttamente da handler
  - Bottoni disabilitati via `canUndo`/`canRedo` da `useModelHistory`

## Regression checklist (pipeline UX v2.5.0 preservata)

- [x] **FeatureButton** funziona — non toccato (50+ callsite invariati)
- [x] **EmptyState** renderizzato in Verify Live + Inspect — i workspace content esistenti ospitati da ShellPanel via passthrough
- [x] **toastApiError** mostra toast italiano — `apiErrors.ts` non toccato
- [x] **ScaleIndicator dinamico** — `ViewportHudRuler` legge da `viewportCameraStore.metersPerScreenHeight` (stesso store del v2.5.7 dinamico)
- [x] **Toggle View deformata/colormap/iso** con preconditions e propose-action — workspace content non modificato
- [x] **Le 16 feature in `lib/preconditions.ts`** continuano a funzionare — registry intatto

## Pattern coerenti

- [x] tutti i font: display=Plus Jakarta Sans, body=Inter, mono=JetBrains Mono
  - `shell.css` linea 38: `body { font-family: "Plus Jakarta Sans", -apple-system, ... }`
  - `shell.css` font-family override: `.tb-tier`, `.tb-ver`, `.tb-model-id`, `.tb-save`, `.sb-item .sb-k`, `.cmd-foot kbd`, ecc. tutti `"JetBrains Mono", monospace`
- [x] singolo accent cyan (#0891B2), no doppio axis
  - `tokens.css`: `--accent: #0891B2` (light), `#6EDDF5` (dark)
  - Nessun secondary accent in `shell.css`
- [x] radius: 4-20px (no più sharp 0)
  - `.rail-btn` radius 10, `.tb-search` 8, `.tb-run` 8, `.sp-icon` 8, `.cmd-modal` 14, `.cmd-item` 8
  - `body.shell-sharp` override a 0 (opt-in via attribute)
- [x] hairline borders + shadow soft (no drammatici)
  - `.vp-hud` shadow: `0 8px 24px -8px rgba(0,0,0,0.15), 0 0 0 1px rgba(0,0,0,0.04)`
  - `.cmd-modal` shadow: `0 24px 64px -12px rgba(0,0,0,0.30)`
  - `.shell-statusbar` border-top hairline 1px
- [x] sentence case + mono uppercase per eyebrow
  - `.sp-section-title` mono 10/700 ws 0.14 uppercase
  - `.tb-tier` mono 9/600 ws 0.06 uppercase
  - `.cmd-group-head` mono 9/700 ws 0.14 uppercase

## Comportamento condizionale (App.tsx)

```ts
const useNewShell = activeId !== null && !isMobile && !isFocusMode;
```

- **Nuova Shell mostrata quando**: utente loggato, modello attivo, desktop ≥768px, NOT in focus mode
- **Chrome legacy mostrato quando**: Dashboard (no model), mobile, focus mode
- Dialog/wizard (HelpSheet, OnboardingTour, NodeDialog, ecc.) sempre montati al root (indipendenti dal chrome)

## Quality gate

| Check | Atteso | Risultato | Status |
|-------|--------|-----------|--------|
| `tsc --noEmit` | 0 errori | 0 errori | ✅ |
| `vitest run` | 685+ (672 + ~15 nuovi) | **687/687** (84 file) | ✅ |
| `vite build` | OK, no >150kB crescita | OK, index 1297 kB (+24 kB vs 1273 baseline) | ✅ |
| `pytest` | 1669 pre-existing | non eseguito (no backend changes) | N/A |

## Verdetto

**Shell rebuild completo: SÌ.**

- 7 componenti nuovi (Shell + ShellTopBar + ShellRail + ShellViewport + ShellPanel + ShellStatusBar + ShellCommandPalette) + 6 HUD overlay
- 1 nuovo CSS (`shell.css` adattato dal mockup, ~992 righe)
- 15 nuovi test (vitest 687/687)
- APP_VERSION bump v2.3 → v2.6
- useTheme hook wired in App.tsx
- Layout root sostituito (condizionale: solo per `model active + desktop + !focus`)
- Pipeline UX v2.5.0 preservata al 100% (no file workspace/store/dialog modificato)

**Pre-requisiti per Fase 3 (Risultati workspace restyled)**: soddisfatti.

## Out of scope Fase 2 (rimandato Fase 3+)

- Refactor workspace content interni (metric tiles, UR-strip, inspector card)
- HelpSheet completa con contenuto guida contestuale
- TrustBadge logica reale (preliminary/draft/validated state)
- Migration `Button` → `Button2` (50+ callsite)
- Rimozione Inter Tight font (dopo migrazione)
- Spostamento GizmoHelper drei in `.vp-gizmo` (oggi placeholder SVG)
- Dark mode UI toggle in Avatar menu
- AvatarMenu real wiring (oggi placeholder "FS")
- Smoke E2E live (in brief separato post-feedback Federico)
