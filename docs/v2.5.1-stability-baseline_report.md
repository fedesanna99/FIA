# v2.5.1-stability-baseline · Report

**Data**: 2026-05-26
**Branch worktree**: `claude/magical-feynman-792f78` (8 commit ahead di `origin/main`)
**SHA HEAD**: `8c08c46` (post-T6)
**Base**: `c32005e` + 2 commit doc precedenti (`0ca43b5` preflight, `77bb772` lookup)
**Tipo**: feat + fix (frontend defensive)
**Stima vs reale**: 1.5–2h previste / ~45 min effettivi (task ben definiti, fix sciolti senza imprevisti)

## Sintesi

ErrorBoundary globale + handler globali errori + 2 QW viewport (gizmo bottom-right + mouseButtons espliciti + preventDefault contextmenu) + fix postcss `@import` order + contract test BackendErrorKind frontend snapshot. Working tree pulito.

## Task completati

| Task | Esito | SHA      | Note |
|------|-------|----------|------|
| T1 ErrorBoundary globale | ✓ | `6ac4e76` | 3/3 test PASS, wrap dentro StrictMode sopra QueryClient |
| T2 handler globali window | ✓ | `3bedcf8` | listener `error` + `unhandledrejection`, console.error `[global ...]` |
| T3 GizmoHelper bottom-right | ✓ | `9abbe42` | una riga `alignment` |
| T4 OrbitControls mouseButtons + ctxmenu | ✓ | `2609548` | `THREE.MOUSE.ROTATE/DOLLY/undefined` + onContextMenu su div wrapper |
| T5 postcss `@import` order | ✓ | `ec0c7ef` | **file vero `frontend/src/index.css`** (NON `precision.css` come da brief — non esiste) |
| T6 contract test BackendErrorKind | ✓ | `8c08c46` | **opzione (b)** snapshot frontend con `it.each` (10 kind) — vedi nota sotto |
| T7 PROJECT_STATE.md | saltato | — | vive in project knowledge chat strategica, non in repo |

## File toccati

| File | Tipo | Δ righe |
|------|------|---------|
| `frontend/src/components/ui/ErrorBoundary.tsx` | nuovo | +79 |
| `frontend/src/components/ui/ErrorBoundary.test.tsx` | nuovo | +47 |
| `frontend/src/main.tsx` | modificato | +18 / −11 (T1+T2) |
| `frontend/src/components/viewport/Viewport3D.tsx` | modificato | +7 / −2 (T3+T4) |
| `frontend/src/index.css` | modificato | +2 / −2 (riordino) |
| `frontend/src/lib/apiErrors.test.ts` | modificato | +42 (appended describe contract) |

## Decisione T6: opzione (b) e perché

Il brief prevede due opzioni per il contract test BackendErrorKind:

- **(a)** Test Python runtime che legge `apiErrors.ts` e ne estrae i kind backend tramite un enum `BackendErrorKind`.
- **(b)** Snapshot frontend con lista hardcoded di kind attesi.

Indagine in 5 min (`grep -rn "\"error\"\s*:\s*\"[a-z_]+\"" backend/`):

- `backend/jobs/worker.py:219` → `"model_not_found"`
- `backend/jobs/worker.py:231` → `"solver_not_dispatched"` ← **NON è in `ERROR_TRANSLATIONS` del frontend**, è un finding aperto
- `backend/billing/middleware.py:27` → `"quota_exceeded"` (sotto chiave `code`)

Il backend **non** espone un enum `BackendErrorKind` consolidato; i kind sono stringhe sparse nei body di errore lanciati da punti diversi. STOP rule #2 del brief: "NON inventare un enum nel backend (è refactor architetturale fuori scope)" → ho scelto **opzione (b)** snapshot frontend.

Forma implementata: `it.each(EXPECTED_BACKEND_ERROR_KINDS)` con asserzione `result.title !== "Errore del solver"`. Ogni kind è un test indipendente: se un mapping viene rimosso il test fallisce nominando esattamente il kind colpevole. Tale scelta produce **+10 test vitest** invece di +1 (vedi quality gate sotto).

TODO commento nel test: `v2.5.x roadmap → consolidare enum runtime backend e promuovere a opzione (a)`.

## Finding bonus (non in scope, da segnalare)

Durante T6 ho scoperto che `backend/jobs/worker.py:231` emette il kind `"solver_not_dispatched"` (caso: solver name non presente nel dispatcher) ma **non è in `ERROR_TRANSLATIONS` del frontend** (`frontend/src/lib/apiErrors.ts:22-32`). L'utente, in caso di errore di questo tipo, vede `Errore del solver · solver_not_dispatched` invece di un messaggio italiano dedicato. Non l'ho aggiunto al mapping perché non è in scope T6 (è bug/debt nuovo). Va aperto come task minore separato.

## Quality gate

| Suite | Risultato attuale | Atteso da brief | Esito |
|-------|-------------------|-----------------|-------|
| pytest | **1669 PASS / 1 FAIL / 7 skipped** (116.86s) | 1670 PASS atteso (con opzione a) | ✓ invariato vs baseline (opzione b non aggiunge pytest) |
| vitest | **597 PASS** in 72 file (21.66s) | 587 atteso (584 + 3 ErrorBoundary) | ✓ +13 totali (3 ErrorBoundary + 10 contract `it.each`) |
| tsc | **0 errori** | 0 | ✓ |
| build | **OK** in 17.55s, **niente warning postcss** | OK senza warning postcss | ✓ chunk-size storico resta |

Il FAIL pytest residuo è `tests/services/providers/elevation/test_elevation_integration.py::test_real_usgs_outside_us_raises` (pre-esistente, già documentato in preflight, non causato da S0).

## Comportamento BEFORE/AFTER

### ErrorBoundary (T1)
- **BEFORE**: throw in qualsiasi sub-tree React → white-screen totale, nessuna informazione utente.
- **AFTER**: throw catturato → fallback italiano (`role="alert"`) con titolo "Si è verificato un errore imprevisto.", paragrafo descrittivo, button "Ricarica" che invoca `window.location.reload()`. Stili inline con `var(--*)` fallback per garantire rendering anche se i token CSS non sono caricati.

### Handler globali (T2)
- **BEFORE**: promise rejection silenti perse, `window.onerror` non agganciato → errori native non tracciati.
- **AFTER**: ogni rejection/error native → `console.error` con prefisso `[global error]` / `[global unhandledrejection]`. Pronto per upgrade a toast in S1 cluster B (infrastruttura Toaster già esiste).

### Viewport (T3+T4)
- **BEFORE**:
  - Gizmo orientamento in **top-right** (sovrapposizione con toolbar topbar in alcune risoluzioni).
  - Tasto destro su canvas → pan camera (override di default browser, comportamento confuso vs IDE 3D standard).
- **AFTER**:
  - Gizmo **bottom-right** (libera area top, segue convention Blender/Rhino/Fusion360).
  - Tasto sinistro = rotate, tasto centrale = dolly/pan, tasto destro = **neutro** (`preventDefault` blocca il browser context menu, OrbitControls riceve `RIGHT: undefined`). Spazio libero per il futuro context menu vero (cluster G post DEC-1).

### Contract BackendErrorKind (T6)
- **BEFORE**: backend o frontend possono drift indipendentemente. Se qualcuno rimuove `missing_constraints` da `ERROR_TRANSLATIONS`, utente vede `Errore del solver · missing_constraints` senza alcun segnale CI.
- **AFTER**: vitest fallisce per ogni kind rimosso dal mapping (`kind 'missing_constraints' ha traduzione italiana dedicata`). Sub-test indipendenti via `it.each` → fallback minimo, error message diagnostica chiara.

### postcss `@import` (T5)
- **BEFORE**: build warning `[vite:css][postcss] @import must precede all other statements` su `frontend/src/index.css:20` (introdotto da PR1 Precision, commit `c78dbed`).
- **AFTER**: `@import url(fonts...)` spostato a riga 1, prima delle direttive `@tailwind` e del commento di intestazione. Build pulito sul fronte CSS. Nessun cambiamento visivo: il font resta caricato (l'ordine `@import` non cambia il rendering, solo l'avviso PostCSS).

## Backward compatibility

Tutte le modifiche sono additive o cosmetiche:

- ErrorBoundary è inerte se nessun throw. Non altera prop tree.
- Handler globali sono listener `addEventListener` non-bloccanti, side-effect solo console.
- mouseButtons `LEFT: ROTATE` e `MIDDLE: DOLLY` corrispondono ai default di OrbitControls — esplicitazione, non cambio di comportamento. Solo `RIGHT: undefined` modifica il default (era `MOUSE.PAN`).
- onContextMenu sul `<div>` wrapper non interferisce con eventi React Three Fiber: il Canvas riceve normalmente tutti gli altri eventi.
- `@import` riordinato è zero-impact a runtime.
- Contract test è additive (10 nuovi test, nessun test esistente toccato).

## STOP scattate

Nessuna STOP rule attivata. Caso prossimo:

- **STOP rule #3 (postcss path)**: brief diceva `frontend/src/styles/precision.css`. Il file **non esiste**. Indagine `find . -name "precision.css"` → solo `docs/redesign-precision/handoff/precision.css` (bundle docs, non sourced) e `docs/redesign-architetti/handoff/precision.css`. `grep` per `@import url(fonts.googleapis.com)` in CSS → 6 file, di cui solo `frontend/src/index.css` è effettivamente importato. Risolto in <2 min, applicato fix sul file vero, niente STOP.
- **STOP rule #2 (no enum backend)**: confermato, scelto opzione (b) come da brief.

## Git history

```
8c08c46 test(errors): contract test BackendErrorKind backend↔frontend (v2.5.1 T6, ARCH-4)
ec0c7ef fix(css): postcss @import order in index.css (v2.5.1 T5)
2609548 fix(viewport): OrbitControls mouseButtons espliciti + preventDefault contextmenu (v2.5.1 T4, QW-3)
9abbe42 fix(viewport): GizmoHelper alignment bottom-right (v2.5.1 T3, QW-1)
3bedcf8 feat(safety): handler globali window.onerror + onunhandledrejection (v2.5.1 T2)
6ac4e76 feat(ui): ErrorBoundary globale con fallback italiano (v2.5.1 T1)
77bb772 docs(lookup): v2.4.9 descrizione operativa bug #2 + bug #9
0ca43b5 docs(preflight): v2.4.9 discovery report pre-S0
c32005e Merge pull request #1 from fedesanna99/feature/redesign-precision
```

## Sync + tag · stato attuale

**Non eseguiti.** Sto operando da un **worktree** sul branch `claude/magical-feynman-792f78`, non direttamente da `test`. Il brief assume operatività diretta su `test` per il sync + tag (`git push origin test:test`, `test:main`, `git tag v2.5.1-stability-baseline`).

Opzioni per il finale (decisione Federico):

1. **Fast-forward merge nel locale**: Federico (o io con go-ahead) fa `git checkout test && git merge --ff-only claude/magical-feynman-792f78` poi sync standard + tag.
2. **Push del branch worktree**: `git push origin claude/magical-feynman-792f78` + PR/merge GitHub-side → poi tag.
3. **Push diretto su test**: `git push origin claude/magical-feynman-792f78:test` + `:main` (forzando il rename, ma poi il worktree resta su feature branch).

Raccomando **opzione 1** per coerenza con `test` come branch di integrazione. Lascio la decisione a Federico per evitare uno share-state push non concordato.

## Prossimo passo raccomandato

Brief `v2.5.2-export-undo-investigation` — investigation report per bug #2 (3 path UI export) e bug #9 (wiring dialog → modelStore.history). NO fix in quel brief: solo mappatura caller + file:linea precisi (`ReportExportDialog.tsx`, `NodeDialog.tsx`, voci CommandPalette correlate, hook che orchestrano la mutation), così l'eventuale brief di fix che segue può andare diretto al punto.
