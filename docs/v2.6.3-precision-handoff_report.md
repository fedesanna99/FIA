# v2.6.3-precision-handoff · Report rollup

**Data**: 2026-05-26
**SHA HEAD**: `e8a597f`
**Tag**: `v2.6.3-precision-handoff` (rollup di 5 sprint atomici)
**Branch sync**: `design-rebuild/v2.6` = `origin/test` = `e8a597f` ✅
**Branch main**: ⚠️ NON syncato (push annullato dall'utente) — sync manuale richiesto
**Deploy**: release **v89** su https://fea-pro.fly.dev/
**Image**: `registry.fly.io/fea-pro:deployment-01KSJD83YG2B8GACS5WGVZNRFK` (387 MB)
**Machine**: `d8d2949c042208`, region `fra`, state **started**
**Smoke E2E live**: **3/3 PASS** in 57.6s contro v89

## Sintesi

Integrazione handoff Precision v2.0 al 100% (Opzione 1 confermata).

- **7 componenti drop-in** (TrustLayerBadge, PercorsoStep, InsightPanel,
  ChecksRail, ChecksDetailTable, ModelsTable, WorkspaceLayout) — già
  presenti in src/components/shell/ dalla sessione precedente (May 26 06:01)
  + **31 nuovi test vitest** per coverage completa
- **Wiring nei consumer** già completato in PR15-16 precedenti:
  ChecksRail+Table in VerifyChecksLive, PercorsoStep in
  PercorsoFullScreenDemo, ModelsTable in ModelliBrowser, TrustLayerBadge
  banner in ReportExportDialog, InsightPanel in ResultsInsightAuto
- **Shell custom** in `frontend/src/shell/` **resta intatta** (Opzione 1).
  La nuova Shell desktop-first prosegue invariata.
- **Mapping Tailwind** Precision → Soft v2.1 esteso con `caretColor.accent`
  e keyframe/animation `precision-pulse-ring` (consumer Sprint 5 T2
  carry-over)

## 5 sprint atomici (cronologia + tag)

| Sprint | Tag | SHA | Tipo | Quality gate |
|--------|-----|-----|------|--------------|
| S1 | `v2.6.3.0-precision-tokens` | `94fa369` | feat (Tailwind utility +12 righe) | tsc 0 · vitest 695/695 · build 1300 kB |
| S2 | `v2.6.3.1-precision-dropin` | `df0be8f` | test (+31 vitest) | tsc 0 · vitest **726/726** · build 1300 kB |
| S3 | `v2.6.3.2-precision-verify-percorsi` | `429aca6` | docs (validation) | tsc 0 · vitest 726/726 · build invariato |
| S4 | `v2.6.3.3-precision-dashboard-report` | `5d2b547` | docs (validation+decisioni skip) | tsc 0 · vitest 726/726 · build invariato |
| S5 | `v2.6.3.4-precision-anim-responsive` | `2c85068` | docs (validation+carry-over Tier 4) | tsc 0 · vitest 726/726 · build invariato |
| **Rollup** | **`v2.6.3-precision-handoff`** | **`e8a597f`** | chore (PROJECT_STATE + APP_TAG) | tsc 0 · vitest 726/726 · smoke E2E **3/3 PASS** |

## File toccati (riassunto compound)

| Categoria | File | Diff |
|-----------|------|------|
| Tailwind config | `frontend/tailwind.config.js` | +12 righe (caretColor + precision-pulse-ring) |
| Test vitest | `frontend/src/components/shell/{TrustLayerBadge,PercorsoStep,InsightPanel,ChecksRail,ChecksDetailTable,ModelsTable,WorkspaceLayout}.test.tsx` | 7 nuovi file, +402 righe, +31 test |
| Version metadata | `frontend/src/lib/version.ts` | APP_TAG bump v2.6.2-shell → v2.6.3-precision-handoff |
| Project state | `docs/PROJECT_STATE.md` | new file (template aggiornato post-compound) |
| Sprint reports | `docs/v2.6.3.{0,1,2,3,4}-*.md` + `docs/v2.6.3-precision-handoff_report.md` | 6 report (~700 righe documentazione) |

**Backend**: zero modifiche in tutto il compound (pytest 1677 collected
invariato, no test rerun necessario).

## Quality gates finali

| Check | Atteso | Risultato | Status |
|-------|--------|-----------|--------|
| `tsc --noEmit` | 0 errori | 0 | ✅ |
| `vitest run` | 695 + N nuovi PASS | **726/726** (91 file, +31 vs baseline) | ✅ |
| `vite build` | OK, ≤ +20 kB gzip | OK (main 1300.70 kB / gzip 382.45 kB, **delta 0 kB**) | ✅ |
| `playwright smoke live` (3 test) | PASS | **3/3 PASS** in 57.6s vs release v89 | ✅ |
| `flyctl deploy` | release nuova attiva | release **v89** live (machine started fra) | ✅ |
| `curl /api/health` | 200 status:ok | `{"status":"ok","version":"1.0.0"}` | ✅ |

## Sprint 3-5 · Decisioni scope-creep documentate

Tutti gli sprint S3-S5 sono chiusi come "docs/validation" perché il wiring
critico era già stato fatto in PR15-16 precedenti (drop-in dei 7
componenti). Le decisioni di **skip** scope-creep sono documentate
esplicitamente nei singoli report:

| Sprint | Skip | Motivo | Carry-over |
|--------|------|--------|------------|
| S3 T3 | PercorsiBeamWizard wrap `PercorsoStep` | Dialog modale, non pagina full-screen; usa già `PercorsoStepper` | – |
| S4 T3 | `TrustLayerBadge variant="watermark"` in ReportExportDialog | Niente `<PdfPreview>` iframe inline | PDF preview + watermark (~2-3h) |
| S4 T4 | `InsightPanel` inline in MakePanel hub | Ridondante con FeatureButton preconditions tooltip | MakePanel root status banner opzionale (30 min) |
| S4 T5 | Pattern hub-card axis-tag `/ Make · {category}` | `PanelHubNav` primitive condiviso da Make/Verify/Solve/Tools, cambio MakePanel-only romperebbe uniformità | Sprint dedicato `v2.6.4-hub-axis-tag` (~1-2h) |
| S5 T2 | OnboardingTour spotlight refactor | Welcome-modal con 9 step descrittivi ≠ spotlight tour | Sprint dedicato `v2.6.5-onboarding-spotlight` (~4h) |
| S5 T3 | Mobile rebuild dentro nuova Shell | Mobile su codepath legacy by design (`useNewShell` ha `!isMobile`) | Sprint dedicato `v2.6.4-mobile-shell` Strada A (~3-4h) |

## Stato sync branch (post-rollup)

| Branch | SHA | Note |
|--------|-----|------|
| `design-rebuild/v2.6` (worktree) | `e8a597f` | HEAD del compound |
| `origin/test` | `e8a597f` | ✅ syncato via `git push origin design-rebuild/v2.6:test` |
| `origin/main` | `b276421` | ⚠️ NON syncato (push annullato dall'utente) — sync manuale richiesto via `git push origin design-rebuild/v2.6:main` o tramite trigger globale "sincronizza test con tutto" |
| Tag `v2.6.3.0-precision-tokens` … `v2.6.3-precision-handoff` | – | ✅ tutti 6 i tag pushati a origin |

## Carry-over (debt esplicito per sprint futuri)

### Tier 1 (subito)
- **Sync `origin/main`** a HEAD `e8a597f` (manuale o trigger globale)
- **Sessione Paolo** · validation utente reale su live v89 (~30-60 min)

### Tier 2 (post-Paolo)
- **Strada A · Mobile rebuild dentro Shell** (`v2.6.4-mobile-shell`, ~3-4h)
- **Fase 3 desktop · Workspace content refactor** (`v2.6.4-workspace-content`, ~3-4h)

### Tier 3 (tech debt)
- DEC-A1 workspace store legacy cleanup (1-2h, P1)
- Migration legacy `CommandPalette.tsx` → `usePaletteDispatch` (1-2h, P2)
- 25 callsite `disabled={!model}` → `FeatureButton` (P1)
- 30 caller Categoria B `toastApiError` migration (P2)
- Migration `Button` → `Button2` ~50 callsite (2-3h)
- Rimozione Inter Tight font (15 min)

### Tier 4 (polish opzionale post-Paolo)
- ShellCommandPalette polish e2 (caret-accent, footer counter, close
  animation reverse, ~1h totale)
- OnboardingTour spotlight refactor (`v2.6.5-onboarding-spotlight`, ~4h)
- Hub-card axis-tag pattern uniformato (`v2.6.4-hub-axis-tag`, ~1-2h)
- PDF preview + TrustLayerBadge watermark in ReportExportDialog (~2-3h)
- MakePanel root-level model-ready InsightPanel (30 min, ridondante con FeatureButton)
- Touch gestures viewport mobile (pinch zoom, two-finger pan, ~2h)
- Mobile landscape orientation (~1h)
- Smoke E2E webkit reale (richiede `npx playwright install webkit` su CI)

## Verbatim per Federico

```
✅ Brief v2.6.3-precision-handoff completato.

Tag pushato:        v2.6.3-precision-handoff (+ 5 tag intermedi)
SHA HEAD:           e8a597f
Branch sync:        design-rebuild/v2.6 = origin/test (allineati)
                    origin/main ANCORA a b276421 (sync manuale richiesto)
Deploy:             release v89 su https://fea-pro.fly.dev/
Quality gate:       pytest 1677 (no rerun) · vitest 726/726 · tsc 0 · smoke 3/3 PASS
Image:              registry.fly.io/fea-pro:deployment-01KSJD83YG2B8GACS5WGVZNRFK

Da fare manualmente (utente):
- Push origin/main a e8a597f (es. trigger "sincronizza test con tutto"
  oppure `git push origin design-rebuild/v2.6:main`)
- Sostituire docs/PROJECT_STATE.md nel project knowledge UI con la nuova
  versione in docs/PROJECT_STATE.md (commit e8a597f)
- (opzionale) Verifica visiva live in browser: deploy, Verify workspace,
  Percorsi wizard, Report export, OnboardingTour
```
