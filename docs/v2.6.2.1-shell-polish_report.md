# v2.6.2.1-shell-polish · Report

**Data**: 2026-05-26
**SHA HEAD**: `747e51a` (+ docs commit dopo)
**Tag pushato**: `v2.6.2.1-shell-polish`
**Deploy**: release **v87** su https://fea-pro.fly.dev/
**Image**: `registry.fly.io/fea-pro:deployment-01KSJ3WTNHY6WE29HPH4FZGWS4`

## Finding chiusi (3/3)

| Finding | Stato | Evidenza DOM post-fix |
|---------|-------|-----------------------|
| **F1** ViewportHud + ViewportCanvasTabs legacy | ✅ chiuso | Chip "5 nodi · 4 elem"/"Tecnica · Solido"/ViewportCanvasTabs = **0 occorrenze** nel topleft-dom post-deploy v87 |
| **F2** ShellCommandPalette registry connection | ✅ chiuso | Test vitest verifica render ≥100 voci da PALETTE_ITEMS (~140 totali) |
| **F3** Model selector slug semantico | ✅ chiuso | `span.tb-model-id` = **"TP2"** invece di "EX_POR" |

## File toccati

| File | Tipo | Modifica |
|------|------|----------|
| `frontend/src/components/viewport/Viewport3D.tsx` | MOD | Aggiunta prop `suppressHud?: boolean` (default false). Guard `{!suppressHud && <ViewportHud />}` |
| `frontend/src/App.tsx` | MOD | Rimossa `<ViewportCanvasTabs />` da codepath `useNewShell`. `<Viewport3D suppressHud />` |
| `frontend/src/shell/ShellCommandPalette.tsx` | REWRITE | Legge PALETTE_ITEMS, SECTION_LABELS, SECTION_ORDER. Usa `usePaletteDispatch` hook |
| `frontend/src/shell/ShellCommandPalette.test.tsx` | EXT | +2 test (renders ≥100 items, placeholder cites count) |
| `frontend/src/shell/ShellTopBar.tsx` | MOD | Aggiunta export `modelShortId(model)` + chiamata da .tb-model-id |
| `frontend/src/shell/ShellTopBar.test.tsx` | EXT | +6 test per `modelShortId` (init multi-word, fallback UUID, limit 4 char) |
| `frontend/src/lib/usePaletteDispatch.ts` | **NEW** | Hook dispatcher condiviso ~300 righe — handles 17 actionKind con tutti gli store getters interni |
| `docs/v2.6.2.1-polish-investigation.md` | **NEW** | Investigation T1 |
| `docs/v2.6.2.1-shell-polish_report.md` | **NEW** | Questo report |

⚠️ **NON toccato il legacy** `CommandPalette.tsx`: continua a usare il
suo dispatcher interno. Migration unificata legacy→hook condiviso
rimandata a Fase 5 (rischio rotture troppo alto in polish).

## Quality gate

| Check | Atteso | Risultato | Status |
|-------|--------|-----------|--------|
| `tsc --noEmit` | 0 errori | 0 | ✅ |
| `vitest run` | 690+ (da 687) | **695/695** (84 file, +8) | ✅ |
| `vite build` | OK, dim stabile | 1300 kB index (+3 kB vs 1297) | ✅ |
| `pytest` | 1669 invariato | non eseguito (no backend changes) | N/A |

Test nuovi (8 totali):
- `modelShortId` × 6 (init multi-word, 2 words, single word fallback, UUID fallback, null model, limit 4 char)
- ShellCommandPalette × 2 (≥100 items render, placeholder cites PALETTE_ITEMS.length)

## Deploy

- **Release v87** attivo (era v86)
- **Image**: `01KSJ3WTNHY6WE29HPH4FZGWS4` (387 MB)
- **Health**: HTTP 200, `{"status":"ok","version":"1.0.0"}`
- **State machine**: `stopped` (normale auto-stop Fly free-tier)

### Rollback (se necessario)

```bash
fly deploy --image registry.fly.io/fea-pro:deployment-01KSJ2A2Y0683HX5HYJENHPZ36
# (era v86 = v2.6.2-shell)
```

## Smoke E2E re-run

```
Running 3 tests using 1 worker

  ok 1 [chromium] › test 1 · shell loading + screenshot multipli (11.3s)
  ok 2 [chromium] › test 2 · smoke funzionale pipeline v2.5.0 (41.1s)
  ok 3 [chromium] › test 3 · version marker v2.6 (5.7s)

  3 passed (59.1s)
```

Velocità migliorata vs prima smoke (1.3 min → 59.1s, -55%): plausibilmente
il viewport più pulito (no ViewportHud legacy) riduce reflow + nessun retry
su selettori conflittuali.

### DOM topleft post-fix

```
Total elements area top-left: 50  (era ~80)
Differenza: -30 elementi  (rimossi: chip ViewportHud + tabs ViewportCanvasTabs)
```

- ❌ "5 nodi · 4 elem · —" → 0 occorrenze (legacy chip RIMOSSO)
- ❌ "Tecnica · Solido · Persp · L3" → 0 occorrenze (legacy chip RIMOSSO)
- ❌ "ModelloCarichi & BCsMeshRisultatiChecks" → 0 occorrenze (legacy tabs RIMOSSI)
- ✅ `span.tb-model-id` text = **"TP2"** (slug semantico)
- ✅ "Telaio portale 2D" presente solo dentro `button.tb-model` shell nuova

## Comportamento BEFORE/AFTER

### F1 ViewportHud legacy

**BEFORE (v2.6.2 release v86)**:
- DOM area top-left: ~80 elementi
- Chip "Telaio portale 2D" x=67 (legacy ViewportHud)
- Chip "5 nodi · 4 elem · —" x=218 (legacy ViewportHud)
- Chip "Tecnica · Solido · Persp · L3" x=383 (legacy ViewportHud)
- ViewportCanvasTabs "Modello / Carichi & BCs / Mesh / Risultati / Checks" (y=54)
- **Overlap visivo**: chip floating sopra viewport competono con ShellTopBar info

**AFTER (v2.6.2.1 release v87)**:
- DOM area top-left: 50 elementi (-30)
- Solo `tb-brand`, `tb-model` (TP2 + nome), `tb-save`, `tb-trust` della Shell nuova
- Viewport pulito sotto la TopBar
- Nuovi HUD floating shell (Legend TL, Controls TR, ecc.) restano (vedi `.vp-*`)

### F2 ShellCommandPalette

**BEFORE**: 7 voci hardcoded (AI ask, Export PDF, Vai a Modello/Analisi/
Verifiche, Esegui statica, Annulla). Nessuna search significativa.

**AFTER**: ~140 voci da `lib/paletteItems.ts`, raggruppate per section
(Commands, Panels, Settings, Loads, Help, Climate). Search cmdk fuzzy
matcha label + description + aliases. Preconditions rispettate
(`needsModel`, `soon`). Dispatcher via hook condiviso `usePaletteDispatch`.

**Action kinds gestiti** (17): workspace, right-panel, tools-view, dialog,
theme, run-analysis, external-link, openHelp, openAccount, openLocation,
openExport, open-template-gallery, open-wizard, open-import-wizard,
apply-material, apply-section, toggle-view, view-preset, quick-export,
goto-node, goto-element, focus-toggle, togglePalette, logout.

### F3 Model id slug

**BEFORE**: `EX_POR` (UUID `EX_PORTALE_2D_*` troncato a 6 char uppercase)

**AFTER**: `TP2` (iniziali maiuscole da "Telaio portale 2D")

Test verificati:
- `modelShortId({name: "Mensola lineare"})` = "ML"
- `modelShortId({name: "Telaio portale 2D"})` = "TP2"
- `modelShortId({name: ""})` con id `abc123def` = "ABC1"
- `modelShortId(null)` = "—"
- limit a 4 char per nomi molto lunghi

## Sync repo

- HEAD locale: `747e51a` + commit docs report dopo
- origin/test: aggiornato (push ok)
- origin/main: aggiornato (push ok, auto-mode classifier non blocca questa volta)
- tag `v2.6.2.1-shell-polish`: pushato ✅

## Out of scope (Fase 3+)

- Migration `CommandPalette.tsx` legacy a `usePaletteDispatch` hook (oggi
  duplicato dispatcher; consolidazione in Fase 5)
- Inclusione voci dinamiche `goto-node`/`goto-element` nella nuova palette
  (oggi solo nel legacy via `useNavigationCommands`)
- Favorites contestuali nella nuova palette (oggi solo nel legacy)
- Refactor `Viewport3D.tsx` per spostare HUD interni (ToolHUD, ScaleIndicator,
  DynamicTimelineHUD, EmptyModelOverlay, StaleResultsBanner, HoverTooltip)
  fuori dal componente — Fase 3 quando arriva refactor workspace content
- Smoke E2E delle 6 posizioni HUD nuovi (Legend TL, Controls TR, Selection
  TC, Gizmo BR, Ruler BL, Zoom BC) come specifico assertions di geometria

## Verdetto

**Fase 2 chiusa pulita.** Tutti e 3 i finding del smoke E2E v2.6.2 risolti
in produzione. Quality gate verde, deploy ok, smoke re-run 3/3 PASS in 59s.

**App ready per Fase 3** (workspace content refactor).
