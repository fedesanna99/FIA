# Path A + B · smoke verify (documentale, no exec)

**Data**: 2026-05-26
**Scope**: T1 brief v2.5.3 — confermare che Path A (button Inspect) e Path B
(palette wiz-report) dell'Export Report sono già funzionanti, come ipotizzato
dall'investigation v2.5.2, prima di applicare i fix Path C.
**Tipo**: verifica statica del codice (lettura sorgenti), niente esecuzione live.

## Path A · Button "Genera report PDF" in ResultsOverviewCard

| Check | File:riga | Esito |
|-------|-----------|-------|
| Button con `onClick={dispatchEvent(...)}` | `frontend/src/components/shell/ResultsOverviewCard.tsx:141-143` | ✓ — `onClick={() => window.dispatchEvent(new CustomEvent("feapro:open-export-pdf"))}` con `data-testid="results-overview-export"` |
| Listener `feapro:open-export-pdf` registrato in App | `frontend/src/App.tsx:289-291` | ✓ — `const openExportPdf = () => setReportExportOpen(true); window.addEventListener("feapro:open-export-pdf", openExportPdf);` dentro `useEffect(() => {...}, [])` (non condizionato, mount-once) |
| Cleanup listener su unmount | `frontend/src/App.tsx:297` | ✓ — `removeEventListener` nel return del useEffect |
| `<ReportExportDialog>` montato | `frontend/src/App.tsx:693-697` | ✓ — `<ReportExportDialog open={reportExportOpen} onClose={() => setReportExportOpen(false)} />` |
| `handleDownload` → `generateReport` + toast | `frontend/src/components/dialogs/ReportExportDialog.tsx:84-103` | ✓ — chiama `generateReport({model, staticResults, modalResults})` poi `notify("success", "Report PDF generato", ...)` |

**Verdetto Path A**: **probabilmente funzionante**. Tutti i 5 hop della catena sono cablati correttamente. Niente listener condizionati, niente dialog non montato, handler con feedback toast. La causa che l'audit v2.3.2 vedesse il path come "morto" più plausibile è (i) il button viene mostrato solo se ci sono risultati statici (`!staticRes` lo disabilita) e l'auditor avrebbe potuto testare senza aver lanciato un'analisi.

## Path B · Voce palette `wiz-report`

| Check | File:riga | Esito |
|-------|-----------|-------|
| Voce palette dichiarata | `frontend/src/lib/paletteItems.ts:277` | ✓ — `{ id: "wiz-report", label: "Apri report export PDF", actionKind: "open-wizard", payload: { wizard: "report" }, needsModel: true }` |
| Dispatcher palette · case `open-wizard` | `frontend/src/components/shell/CommandPalette.tsx:158-171` | ✓ — `useWizardStore.getState().open(wizard, rest)` con `wizard = "report"` |
| Subscribe `wizardStore` in App | `frontend/src/App.tsx:312-357` | ✓ — `useWizardStore.subscribe((state, prev) => {...switch (kind)...})` registrato in `useEffect(() => {...}, [setDialog])` |
| Case `"report"` dispatch event | `frontend/src/App.tsx:349-352` | ✓ — `case "report": window.dispatchEvent(new Event("feapro:open-export-pdf")); break;` |
| Da qui in poi: identico al Path A | `frontend/src/App.tsx:291` listener | ✓ stesso flow |

**Verdetto Path B**: **probabilmente funzionante**. 4 hop tutti in place. La voce ha `needsModel: true`, quindi non parte se non c'è un modello caricato (`CommandPalette.tsx:84` early return su `item.needsModel && !model`). Possibile causa audit "morto": auditor avrebbe potuto provare la voce con modello non caricato (palette comunque mostra la voce, ma il click no-op).

## Verdetto complessivo

Path A: **probabilmente funzionante**, blocco `disabled` legittimo se manca `staticResults`.
Path B: **probabilmente funzionante**, blocco `needsModel` legittimo se nessun modello.

Conferma effettiva via smoke E2E live (apri modello demo → esegui statica → testa Path A button + Path B palette) è scope del brief `v2.5.10-paolo-readiness-gate`. In questo brief (v2.5.3) NON tocchiamo Path A/B perché il modello mentale del fix (toast PDF + XLSX refactor) non li impatta.
