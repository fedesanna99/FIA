# v2.6.3.0-precision-tokens · Report

**Data**: 2026-05-26
**Branch**: design-rebuild/v2.6 (SHA: `94fa369`)
**Tipo**: feat (foundation utility mapping)

## Sintesi
Mapping Tailwind utility classes Precision → CSS vars Soft v2.1. Foundation
additiva per drop-in handoff Precision v2.0. Nessuna sovrascrittura: le
entry esistenti sono preservate.

## Diagnosi T1 (atoms/cn/tokens/PercorsoStepper/useMediaQuery)

| Asset | Stato | Path |
|-------|-------|------|
| UI atoms barrel | ✅ presente | `frontend/src/components/ui/index.ts` esporta Chip+ChipTone, Avatar, Button, Input, EmptyState, Spinner, cn, FormField, FeatureButton, IconButton, Kbd, Skeleton, Toggle, Tabs, Dialog, Tooltip, Badge, DropdownMenu, TipBubble |
| `cn()` helper | ✅ presente | `frontend/src/components/ui/cn.ts` |
| Tailwind config | ✅ presente | `frontend/tailwind.config.js` (Soft v2.1 con bridge ai tokens) |
| `PercorsoStepper` | ✅ presente | `frontend/src/components/shell/PercorsoStepper.tsx` (esporta `PercorsoStepper`, `PERCORSO_STEPS_6`, `PercorsoStep` type — **stesso dir del drop-in handoff, import path `./PercorsoStepper` matcha senza adapt**) |
| `useMediaQuery` | ❌ assente | Opzionale per Sprint 5 (richiesto solo se gap responsive d1-d2 reali) |
| `Chip` API | ✅ compatibile | `tone`, `dot`, `icon`, `className`, `data-testid` via `...rest` — match con handoff TrustLayerBadge |
| `--accent-rgb` | ✅ presente | `tokens.css:46` = `8,145,178` (+ override dark `tokens.css:96`) per `precision-pulse-ring` rgba |

## Diagnosi T2 (utility classes effettivamente usate dall'handoff)

Grep `frontend/src/components/shell/*.tsx` post-drop-in (riferimento `/tmp/handoff/react-pack/shell/*.tsx`):

| Utility | Stato pre-fix | Sorgente |
|---------|---------------|----------|
| `animate-fade-in` | ✅ già mappato | `tailwind.config.js:209` |
| `animate-pulse` | ✅ già mappato (precision-pulse 1.2s) | `tailwind.config.js:215` |
| `animate-slide-down`, `-up`, `-right`, `-left` | ✅ già mappati | `tailwind.config.js:210-213` |
| `border-l-accent`, `-success`, `-warn`, `-danger` | ✅ derivati automaticamente da `colors` top-level | Tailwind borderColor sharing |
| `duration-fast` (120ms), `duration-mid` (200ms) | ✅ già mappati | `tailwind.config.js:158-162` |
| `text-ink-2`, `-3`, `-4` | ✅ già mappati come `ink.{2,3,4}` | `tailwind.config.js:41-43` |
| `tracking-tight-{1..4}`, `tracking-wide-{1..4}` | ✅ già mappati | `tailwind.config.js:148-157` |
| `z-panel` (10), `z-dialog` (40), `z-toolbar` (20) | ✅ già mappati | `tailwind.config.js:174-183` |
| `shadow-pop`, `shadow-dialog`, `shadow-card`, `shadow-hover` | ✅ già mappati | `tailwind.config.js:136-146` |
| `font-display`, `font-mono` | ✅ già mappati | `tailwind.config.js:102-108` |

**Conclusione**: il mapping esistente Soft v2.1 (Fase 1 v2.6.1 foundation +
PR Precision precedenti) copre **100%** delle utility usate nei 7 file
handoff. Non serve refactor.

## T2 · Modifiche additive

| File | Changes | Rationale |
|------|---------|-----------|
| `frontend/tailwind.config.js` | +12 righe | Aggiunto `caretColor.accent` + keyframe/animation `precision-pulse-ring` (consumer Sprint 5 T2 OnboardingTour spotlight) |

Dettaglio modifiche:

```diff
+ caretColor: {
+   accent: "rgb(var(--c-accent))",
+ },
```

```diff
+ "precision-pulse-ring": {
+   "0%, 100%": { boxShadow: "0 0 0 0 rgba(var(--accent-rgb), 0.4)" },
+   "50%":      { boxShadow: "0 0 0 6px rgba(var(--accent-rgb), 0)" },
+ },
```

```diff
+ "precision-pulse-ring": "precision-pulse-ring 1.2s cubic-bezier(.4,0,.2,1) infinite",
```

**NON ho sovrascritto** la `precision-pulse` esistente (opacity-based, usata
dal `animate-pulse` legacy). Il nuovo ring è disponibile come
`animate-precision-pulse-ring`.

## Quality gates

| Check | Atteso | Risultato | Status |
|-------|--------|-----------|--------|
| `tsc --noEmit` | 0 errori | 0 | ✅ |
| `vitest run` | 695 invariato | **695/695** (84 file, 25.81s) | ✅ |
| `vite build` | OK, dim stabile | OK (main 1300.70 kB / gzip 382.45 kB, **delta 0 kB**) | ✅ |

## File toccati (1 commit)

| Commit | File | Cosa |
|--------|------|------|
| `94fa369` | `frontend/tailwind.config.js` (+12 righe) | `caretColor.accent`, keyframe + animation `precision-pulse-ring` (consumer S5) |

## NON toccato

- `tokens.css`, `index.css` (CSS vars già pronti)
- Atoms barrel `components/ui/*` (Chip API già compatibile)
- `PercorsoStepper.tsx` (handoff lo importa con path `./PercorsoStepper` → match diretto post-drop-in)
- Backend, route, test pytest
- Shell custom `frontend/src/shell/*`

## Prossimo passo
Sprint 2 (`v2.6.3.1-precision-dropin`): drop-in dei 7 file handoff in
`frontend/src/components/shell/` + test vitest unitari. Adapt minimo: rename
`PercorsoStep.tsx` → `PercorsoStepTemplate.tsx` per evitare collision col
type `PercorsoStep` esportato da `PercorsoStepper.tsx`.
