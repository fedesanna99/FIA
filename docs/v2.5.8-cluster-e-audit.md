# Cluster E · audit information architecture

**Data**: 2026-05-26
**Scope**: T1 brief v2.5.8 — mappare Make panel, callsite ModelStatsBadge, termini inglesi residui prima di applicare i fix T2-T4.

## 1. Make panel attuale (`shell/panels/MakePanel.tsx`)

**Pattern**: hub-first navigation con drill-in. 5 HUB_CARDS che aprono sub-view interne. **STOP rule #1 ATTIVATA**: pattern strutturato attorno a sub-view interne, refactor completo "azioni dirette" richiederebbe rewriting dell'intera navigation + adeguamento test esistenti. **Scope T2 ridotto** a rinomina sub-text italiano + chiusura doppia destinazione I/O.

| Card | Label attuale | Sub-text attuale (jargon) | Sub-text target italiano | Note |
|------|----------------|----------------------------|--------------------------|------|
| `geometria` | Geometria | "Albero · nodi · elementi · materiali" | "Vista gerarchica del modello" | rimuovo "Albero" (BUG-017) |
| `mesh` | Mesh | "Wizard discretizzazione (line/quad/h8)" | "Genera mesh strutturate (beam · piastra · solido)" | rimuovo "Wizard" + jargon "line/quad/h8" |
| `carichi` | Carichi | "Nodali · distribuiti · climate apply" | "Concentrati · distribuiti · climatici (neve/vento)" | rimuovo "climate apply" (BUG-018) |
| `vincoli` | Vincoli | "Fixed · pinned · rollers · springs" | "Incastri · cerniere · carrelli · molle" | traduzione completa (BUG-018) |
| `io` | Import / Export | "Wizard DXF/IFC/JSON · Tools export" | "Importa DXF · IFC · JSON" | rimuovo Export (è in Tools), card diventa "Importa modello" (BUG-016) |

Tab interni `tab === "io"` (riga 195-222) ha 2 button: "Apri wizard Import" + "Apri Export (rail destro Tools)". Decisione: rimuovo il secondo button (redirect a Tools è ridondante con la nav esistente), lascio solo Import.

## 2. ModelStats callsite duplicati

`grep "nodes.length|elements.length"` su `.tsx`:

| Callsite | File:riga | Pattern | Tipo info |
|----------|-----------|---------|-----------|
| MakePanel `CountsRow` | `shell/panels/MakePanel.tsx:241-257` | grid 4 col `Nodi/Elem./Carichi/Vincoli` | stato build modello |
| StatusBar (bottom bar) | `components/layout/StatusBar.tsx:46-72` | inline `N: x · E: y · DoF: z · 3D · SI` | stato runtime engine |
| AICopilotPanel context | `components/panels/AICopilotPanel.tsx:126` | inline `Contesto: name · N · EL` | contesto AI |
| ComparePanel diff table | `components/panels/ComparePanel.tsx:71-72` | tabella `a vs b` confronto modelli | confronto |
| TemplateGalleryDialog | `components/dialogs/TemplateGalleryDialog.tsx:140-141` | dialog info template | template summary |
| ImportWizard summary | `components/dialogs/wizards/ImportWizard.tsx:473,474,510` | summary import | review pre-import |

**Decisione T3**:
- **Crea** `components/ui/ModelStatsBadge.tsx` (non esiste) con 2 variant: `compact` (1-line "5 nodi · 12 elementi") e `detailed` (grid 4 col nodi/elem./carichi/vincoli).
- **Migra** `MakePanel.CountsRow` → `<ModelStatsBadge variant="detailed">` (sostituzione 1:1).
- **NON migrare** StatusBar (format `N: E: DoF: 3D units` molto specifico runtime engine, non solo stato build).
- **NON migrare** ComparePanel/AICopilot/ImportWizard/TemplateGallery (usi contestuali specifici, non duplicano "ModelStatsBadge generale").

BUG-045 ridondanza topbar/sidebar: chiarimento — **StatusBar è in basso (status bar), non in topbar**. CountsRow è in sidebar (Make panel). Sono effettivamente in 2 posti ma con angolature diverse (build vs runtime engine). Closure parziale via componente condiviso + documentazione: la duplicazione "Nodi/Elementi" è semanticamente diversa, non ridondante puro.

## 3. Termini inglesi target (BUG-018)

`grep "Wire|Solid|Transp|Engine|Tools|Fixed|pinned|rollers|springs|MODELING|CLIMATE LOADS|Wizard"` su `.tsx`:

| Termine | File:riga | Pattern (label UI o contract?) | Traduzione |
|---------|-----------|--------------------------------|-------------|
| `"Wire"` / `"Solid"` / `"Transp"` | `components/panels/ViewportControls.tsx:39` | label UI button | "Wireframe" / "Solido" / "Trasparente" |
| `"Wire"` / `"Solid"` / `"Transp"` | `components/viewport/ViewportHud.tsx:43` | label UI viewport HUD (duplicato) | idem |
| `"Wire"` / `"Solid"` / `"Transp"` | `shell/panels/ViewPanel.tsx:129` | label UI Section | idem |
| `"Engine"` | `shell/panels/ViewPanel.tsx:150,160` | Section title + label UI | "Motore" |
| `"Tools"` | `components/shell/RightRail.tsx:34` | label rail destro | "Strumenti" |
| `"Tools"` | `shell/panels/ToolsPanel.tsx:98` | title PanelChrome | "Strumenti" |
| `"Wizard"` | `MakePanel.tsx:38-43` sub-text | già coperto da T2 rename sub-text | n/a |
| `"Fixed/pinned/rollers/springs"` | `MakePanel.tsx:42` sub-text | già coperto da T2 | n/a |
| `"Climate apply"` | `MakePanel.tsx:41` sub-text | già coperto da T2 | n/a |
| `"MODELING / CLIMATE LOADS"` | (cercati, non trovati come label UI) | OK | non target |

**Scope T4**: 6 modifiche dirette (3 file con Wire/Solid/Transp + ViewPanel Engine + RightRail Tools + ToolsPanel title). I termini in MakePanel sub-text sono coperti da T2. Stringhe come `actionKind: "open-wizard"` o `kind: "fixed"` sono contract (id store/payload) — NON tradurre.

**Wireframe nota**: il brief dice di mantenere "Wireframe" come termine standard CAD. Quindi:
- `wireframe` (lowercase, id viewportMode) → invariato
- `Wire` (label UI abbreviato) → `"Wireframe"` (esteso italiano, accettato come standard)

## 4. Scope finale confermato

- **T2** (BUG-016+017): rinomina sub-text 5 HUB_CARDS in italiano + rimuovi 2° button Export dal tab `io`. NO refactor pattern hub→azioni (STOP rule #1).
- **T3** (BUG-039+045): crea `ModelStatsBadge` shared + migra `MakePanel.CountsRow`. 5 test minimi.
- **T4** (BUG-018): 6 modifiche dirette per termini Wire/Solid/Transp/Engine/Tools.
- **T5**: commit + report.

## 5. Finding aperti out of scope

1. **MakePanel refactor completo hub→azioni dirette**: pattern v2.5.8 brief richiesto ma scope troppo ampio (refactor navigation + adeguamento test). Debt sprint dedicato.
2. **StatusBar dedup con MakePanel CountsRow**: semanticamente non sono ridondanti (build vs runtime engine). Closure documentale.
3. **AICopilotPanel/ComparePanel/ImportWizard/TemplateGallery**: stat inline contestuali, non target T3.
4. **MakePanel tab interni** (Geometria/Mesh/Carichi/Vincoli) hanno ancora termini come "Climate Loads" nell'help text (riga 165) — tradotti solo i sub-text hub, non i contenuti interni. Tradurre in cleanup futuro.
