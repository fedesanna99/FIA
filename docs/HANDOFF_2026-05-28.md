# HANDOFF · v3.1.0 · 2026-05-28

> Resume post-compact per la prossima chat.

---

## Quick state

- **App**: FEA Pro · FEM web studio per ingegneri strutturisti
- **Deploy live**: https://fea-pro.fly.dev/ release **v110** (Fly machine d8d2949c042208)
- **Tag attivo**: `v3.1.0-feature-recovery-design-trapianto`
- **Branch**: `design-rebuild/v2.6` = `origin/test` = `origin/main`
- **Worktree**: `C:\Users\fedes\Desktop\repository\LIA\fea-pro\.claude\worktrees\magical-feynman-792f78`

## Cosa è successo oggi (2026-05-28)

Sessione di ~20h equivalente. Da v3.0.1 (mattina, hotfix Dashboard entry points) a v3.1.0 (sera).

### Mattina · 3 bug critici Dashboard

Federico segnalava: "non riesco a creare/aprire modelli". 3 bug fixati in `v3.0.1-hotfix-dashboard-entry-points`:
- **Bug #1** "Nuovo modello vuoto": `<NewModelDialog>` viveva solo in TopBar legacy (non montato in Dashboard fullscreen). Hostato in App.tsx come singleton.
- **Bug #2** "Apri template": `feapro:load-template` dispatchato ma nessun listener. Aggiunto in App.tsx.
- **Bug #3** Template ID mismatch: TemplatesPage usava `ex_uc1` etc. ma backend serve `ex_simple_beam_2d`. Aggiunto mapping `backendId` + filtro graceful per 4 template non-wireati (UC5/UC8/UC9/UC10).

### Pomeriggio · Audit completo (6 audit paralleli)

Lanciati 6 agenti audit in parallelo:
- **B** Backend endpoint orfani: 16 LIVE / 9 HALF-WIRED / 2 NO-USAGE su 27 endpoint
- **C** 4 chrome paralleli: Shell custom / chrome legacy / Studio v2 / Dashboard. Decision tree completo
- **D** Studio v2 completeness: 10-25% wireato per page, StudioShell 14 elementi hardcoded
- **A** Mappa entry-point: 50 features → 11 INTUITIVE · 27 NASCOSTE · 18 SEPOLTE
- **E** Mobile: 9 route OK · 4 Studio partial · 1 broken (/design/states)
- **F** Test impact: 10 test eliminerebbero col cleanup, 0 E2E

### Sera · Piano operativo eseguito (3 fasi su 4)

**Fase 2 · Recupero 10 feature sepolte** (tag `v3.1.0-fase-2-feature-recovery`):
- 2a · Rich AvatarMenu in Shell custom (Focus/Account/Location/Theme/Export/Logout)
- 2b · Settings link in AvatarMenu
- 2c · ViewPanel come 6° workspace nella rail (deformata/colormap/diagrammi/grid)
- 2d · 4 Climate Loads buttons in MakePanel (Vento/Neve/Sismica/Altimetria)
- 2e · Custom material/section CTA in Make hub
- 2f · Percorsi guidati link in AvatarMenu
- 2g · 5 panel orfani wirati (Buckling/FFT/Spectrum/ModeSup/ZZError) come hub-card Inspect

**Fase 1 · Trapianto estetico Shell custom** (tag `v3.1.0-fase-1-3-design-trapianto`):
- Nuovo `frontend/src/styles/shell-mockup-v3.css` (310 righe override)
- Replica look mockup Studio v2 `.s-*` su classi Shell custom `.shell-*/.tb-*/.sp-*/.sb-*`
- Riskinning: TopBar (brand square accent + pill mockup + save chip + trust badge dot pulsante + cmd 34h + run gradient + iconbtn 32×32) · Rail collapsed (40×40 + strip 3px attivo) · Panel (head 28×28 icon + tabs underline accent) · StatusBar (mono 10px + sep + dot)
- ZERO modifiche JSX. CSS cascade vince. Reversibile.

**Fase 3 · Studio v2 → showcase**:
- 4 pages `/studio/{modello,analisi,verifiche,io}` ora con `<ShowcaseBanner>` top giallo
- Restano accessibili come vetrina design ma non confondono utente operativo

**Fase 4 · Cleanup file morti**: **SKIPPATA** (per scelta utente). I 17 file orfani identificati dall'audit (Dashboard legacy, Sidebar, Toolbar, 3 *Workspace, EditorBar, AnalysisSettings, SelectionInspector, MaterialsLibrary, ValidationPanel, SnapshotsPanel, ViewportControls, ViewPanelContent, OnboardingTour legacy) sono RIMASTI in repo per safety. Pulizia rimandata.

## 13 route live + 4 legal pages

```
/login /signup /forgot-password /verify-email  (4 auth · 98%)
/                                              (Dashboard mockup-driven · 98%)
/templates                                     (gallery · 95%)
/percorsi/uc1                                  (stepper · 85%)
/studio/modello /studio/analisi /studio/verifiche /studio/io  (4 showcase con banner)
/settings                                      (8 tab · 1 funzionale + 7 placeholder)
/design/dialogs /design/states /design/mobile  (3 showcase)
/privacy /terms /about /preliminary            (4 legal)
```

## Pipeline tag 2026-05-28

| Tag | Cosa |
|---|---|
| `v3.0.1-hotfix-dashboard-entry-points` | 3 fix critici Dashboard mattina |
| `v3.1.0-fase-2-feature-recovery` | 10 feature sepolte rese accessibili |
| `v3.1.0-fase-1-3-design-trapianto` | Shell custom riskinned + Studio v2 showcase |
| **`v3.1.0-feature-recovery-design-trapianto`** | **Release ufficiale finale (rollup)** |

## Bug residui noti (tech debt, NON bloccanti)

### Architetturali (Fase 4 saltata)

- 17 file morti puri identificati ma NON eliminati per safety user
- `Dashboard.tsx` legacy ancora attiva su mobile catch-all `/` (non sostituita da DashboardPage mockup)
- 4 chrome paralleli convivono ancora (Shell custom + chrome legacy + Studio v2 + Dashboard)
- Studio v2 listener `feapro:*` non funzionano (App.tsx bypassato) — non rilevante ora che è showcase
- CSS asymmetry body lockscroll mobile (`:has(.studio)` aperto, `:has(.shell.shell-soft)` no)

### Visivi minori

- Avatar in Shell custom usa CollabAvatars rich (gradient) invece di `.s-avatar` square del mockup
- `/design/states` zero `@media` mobile → broken su mobile
- Studio v2 StudioShell 14 elementi hardcoded (nome modello, save, version, counters) — non rilevante ora che è showcase

### Backend orfani

- `POST /api/postprocess/{id}/isolines` — nessun caller frontend
- `POST /api/analysis/seismic_th/{id}` sync wrapper — dead code (l'app usa async via jobsApi)
- `POST /api/analysis/rayleigh` — caller solo in AnalysisSettings orfano
- `GET /api/geocoding/reverse` + `GET /api/geocoding/best` — esportati ma 0 caller

### Feature wireate ma UX incompleta

- 5 panel `Buckling`/`FFT`/`ResponseSpectrum`/`ModeSuperposition`/`ZZError` wirati in InspectPanel ma UX scarna (panel originali con stato base)
- Settings 8 tab: 7 placeholder "in arrivo v3.x"
- StudioShell `.tb-avatar` non override (custom gradient ok ma differente da mockup)
- iOS notch min-height edge case in alcune route

### Carry-over commerciale

- Stripe live billing (~3-5gg)
- Test sessione Paolo
- Catalogo tubolari custom (~2-3gg)
- WCAG 2.1 AA full audit
- Backend timestamp sort Recent
- Jobs cronologia panel UX

## Workflow consolidato

Per i prossimi sprint, pattern consolidato:
1. **Audit before fix** (sempre — anche piccoli) → meglio capire mappa
2. **Funzionale prima dell'estetico** → Fase 2 (recovery) prima di Fase 1 (CSS)
3. **Push frequenti `test`, no deploy intermedi** → safety net senza attese 10 min
4. **Tag granulari intermedi + tag rollup finale** → granularità per rollback
5. **CSS override file dedicato** (`shell-mockup-v3.css`) → reversibile, niente touch al CSS originale

## File chiave per ripartire

**Leggi nell'ordine**:
1. `docs/HANDOFF_2026-05-28.md` — questo file
2. `docs/PROJECT_STATE.md` — stato live aggiornato
3. `docs/audit-css-shell-wrapper-2026-05-27.md` — audit CSS shell
4. `docs/audit-mockup-completeness-2026-05-27.md` — gap analysis

## Esempi prossimi prompt utili

- "Apri https://fea-pro.fly.dev/ e fai user testing v3.1.0"
- "Avvia Fase 4 cleanup ora che siamo confident"
- "Wire Settings 7 tab placeholder con contenuto reale"
- "Riskinning Avatar in Shell custom matchando .s-avatar mockup"
- "Investigare warning Fly 'app not listening on 0.0.0.0:8000' (probabile falso positivo)"
- "Eliminare endpoint backend orfani (rayleigh sync, seismic_th sync, isolines, geocoding/reverse, geocoding/best)"
- "Avvia integrazione Stripe live billing"

## Numeri sessione 2026-05-28

- **3 commit** sopra v3.0.0 (504daad fix · 5693394 Fase 2 · cf080a5 Fase 1+3)
- **+1149 / -23** linee diff (in 22 file)
- **4 nuovi tag** Git
- **2 deploy** Fly.io (v109 mattina v3.0.1 · v110 sera v3.1.0)
- **6 audit doc** in `docs/`
- **860/860 vitest** verde a fine sessione
- **0 endpoint backend** modificati (zero rischio regressione FEM solvers)

---

**Conclusione**: app **production-ready ~90%** funzionalmente, **~95%** visualmente. Salto enorme di qualità dal pomeriggio (v3.0.1 dashboard broken) alla sera (v3.1.0 design + feature recovery completa). Tech debt restante ben mappato e documentato per future iterazioni.
