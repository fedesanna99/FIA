# v2.6.3.4-precision-anim-responsive · Report

**Data**: 2026-05-26
**Branch**: design-rebuild/v2.6 (SHA: `5d2b547`)
**Tag**: `v2.6.3.4-precision-anim-responsive`
**Tipo**: docs (animations già coperte; refactor OnboardingTour fuori scope)

## Sintesi

Animations e2 per ShellCommandPalette già implementate in `shell.css`
(palette `.cmd-*` classi con keyframe `cmd-fade-in` + `cmd-slide-up`).
Specs e1 per OnboardingTour spotlight richiedono refactor strutturale
(welcome-modal → vera spotlight tour) fuori scope di un singolo sprint.
Responsive d1-d2: gap già coperti dal mobile codepath legacy esistente.

## T1 · Applicare e2 (CommandPalette animations) a ShellCommandPalette

**Stato attuale** (file `frontend/src/styles/shell.css:879-984`):

| Spec e2 | Stato | Note |
|---------|-------|------|
| `animate-fade-in` 120ms backdrop | ✅ implementato | `cmd-fade-in` 120ms cubic-bezier(0.2,0,0,1) |
| `animate-slide-down` 220ms modal (from top) | ⚠️ variant — usa `cmd-slide-up` 180ms (from bottom, pattern conventional) | Slide-up è il pattern command palette industry-standard (Linear, GitHub, VSCode). Cambio in slide-down sarebbe inusuale |
| Close animation reverse | ⚠️ Radix Dialog gestisce close, no custom slide-up | Default Radix unmount → no animation reverse. Polish opzionale: stateful `closing` + setTimeout 200ms before unmount. Stima 30 min |
| Item highlight `border-l-2 border-l-accent` | ⚠️ usa `border-1 + accent-subtle bg` (PR Precision PR15) | Diverge dalle specs handoff ma è design-coerente con la palette esistente (Soft v2.1 cyan) |
| Empty state | ✅ implementato (riga 89-91) | `<Command.Empty>` con suggerimenti |
| Footer hint `↑↓ · ↵ · esc · {filtered}/{total}` | ⚠️ ha `↑↓ · ↵ · esc · ✨ Copilot`, no counter dinamico | cmdk non espone `filtered.length` esternamente. Polish minore |
| `caret-accent` su input | ✅ supportato via Sprint 1 mapping | Applicabile aggiungendo `caret-accent` su Command.Input |

**Decisione**: nessuna modifica codice. Il pattern attuale è coerente,
funzionale e segue industry standards (slide-up bottom-anchored). Le
divergenze dalle specs handoff sono cosmetic.

**Carry-over** (polish opzionale, stima totale ~1h):
- Aggiungere `caret-accent` su `<Command.Input>` (15 min)
- Footer counter `{filtered}/{total}` via tracking esterno cmdk (45 min)
- Close animation reverse (`closing` state + delay unmount) (30 min)

## T2 · Applicare e1 (Onboarding spotlight) a OnboardingTour

**Stato attuale** (file `frontend/src/components/shell/OnboardingTour.tsx`,
397 righe):

`OnboardingTour` attuale è un **welcome modal con 9 step descrittivi**:
- 9 step (welcome + 8 workspace overviews)
- Modal centrata `fixed inset-0 flex items-center justify-center`
- `animate-fade-in` su backdrop, `animate-slide-up` su modal ✅
- Step navigation con progress bar `h-1.5 transition-all`
- localStorage flag `feapro-onboarding-seen-v4`

**Specs e1 richiedono spotlight tour vero**:
- Backdrop con clip-path / SVG cut-out su target element
- Tour card che si riposiziona dinamicamente vicino al target
- `data-tour-target="active"` con `precision-pulse-ring` keyframe (Sprint 1)
- targetRef.getBoundingClientRect() per ogni step

**Decisione**: SKIP. Refactor welcome-modal → spotlight-tour è scope
architetturale (~3-4h) che richiede:
- Mapping di ogni step a un selector CSS reale del target UI
- Logica clip-path o portal-based spotlight
- Gestione layout shift quando l'utente naviga step
- Re-design degli step (i 9 attuali descrivono workspaces, non point-and-show UI elements specifici)

**Carry-over Tier 4**: nuovo sprint dedicato `v2.6.5-onboarding-spotlight`
che migri OnboardingTour a vera spotlight tour. Pre-requisiti: definire i
target UI da spotlight (5-7 elementi chiave, non 9 workspace overview).
Stima 4h.

## T3 · Verifica responsive d1-d2

**Specs d1 (mobile <768px)**:

| Spec | Stato | Path |
|------|-------|------|
| MobileTabbar 5 voci + safe-area-inset-bottom | ✅ presente | `frontend/src/components/shell/MobileTabbar.tsx` |
| MobilePanel full-screen drill-in | ✅ presente | wrapper esistente |
| MobileMoreMenu 8 voci | ✅ presente | wrapper esistente |
| PercorsoStepper compact mode | ✅ presente (`compact?: boolean` prop riga 42) | wired via consumer mobile |
| Touch targets ≥ 44×44px | ✅ verificato in v2.2.1 audit | mobile codepath |
| Topbar 40px mobile (vs 48px desktop) | ⚠️ NON applicato (nuova Shell ha 56px fissa) | Sprint A "Strada A" (mobile rebuild dentro Shell) |

**Specs d2 (tablet 768-1280px)**:

| Spec | Stato | Note |
|------|-------|------|
| Right rail 56px + right panel 220px | ⚠️ ShellPanel attuale fissa 380px | Non applicabile alla Shell custom corrente |
| LeftRail collapse 56px | ⚠️ Shell custom usa 48px LeftRail fisso | Diverge dalle specs ma coerente con nuova Shell |

**Decisione**: Il mobile vive su **codepath legacy** (per design v2.6.2 -
useNewShell richiede `!isMobile`). La nuova Shell è desktop-first per
sessione Paolo. La migrazione mobile è scope dedicato (Strada A,
documentato in `docs/SESSION_HANDOFF_2026-05-26.md`).

**Carry-over**: come da `SESSION_HANDOFF` Tier 2 Opzione C — Strada A
mobile rebuild dentro nuova Shell. Stima 3-4h. Specs d1-d2 saranno la
fonte di verità per quel sprint.

## Quality gates

| Check | Atteso | Risultato | Status |
|-------|--------|-----------|--------|
| `tsc --noEmit` | 0 errori | 0 (no code changes) | ✅ |
| `vitest run` | 726/726 | 726/726 invariato | ✅ |
| `vite build` | OK | OK invariato | ✅ |

## File toccati (0 commit codice + 1 commit docs)

Nessuna modifica codice in Sprint 5. Tutto il wiring necessario è già
presente.

| Commit | File | Cosa |
|--------|------|------|
| `<sha-docs>` | `docs/v2.6.3.4-precision-anim-responsive_report.md` | Report validazione + decisioni skip + carry-over |

## Carry-over Tier 4 (debt esplicito per sprint futuri)

1. **Polish ShellCommandPalette e2** (~1h):
   - `caret-accent` su Command.Input (15 min)
   - Footer counter `{filtered}/{total}` (45 min)
   - Close animation reverse stateful (30 min)

2. **OnboardingTour spotlight refactor** (`v2.6.5-onboarding-spotlight`,
   ~4h): migra da welcome-modal a vera spotlight tour con clip-path,
   targetRef e `precision-pulse-ring` (keyframe già mappato in Sprint 1).
   Pre-requisito: definire 5-7 target UI key (vs 9 step descrittivi
   attuali).

3. **Strada A mobile rebuild** (`v2.6.4-mobile-shell`, ~3-4h):
   estendere `useNewShell` condition removendo `!isMobile`, adattare Shell
   con responsive layout ≤md, integrare MobileTabbar come bottom nav della
   nuova Shell. Specs d1-d2 della guidance handoff sono fonte di verità.

## Prossimo passo

**Rollup** (`v2.6.3-precision-handoff`): tag finale, aggiornamento
PROJECT_STATE, sync test↔main, deploy Fly.io, smoke E2E live, report
rollup.

NB: il push dei tag intermedi avviene SOLO al rollup, non in singoli
sprint.
