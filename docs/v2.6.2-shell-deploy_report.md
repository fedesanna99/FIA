# v2.6.2-shell · Deploy report

**Data**: 2026-05-26 ~12:00 UTC
**Tipo**: deploy production
**Tag pushato**: `v2.6.2-shell` (SHA `03d9bd7`)
**Branch deployato**: `design-rebuild/v2.6` (in sync con `origin/main` e `origin/test`)

## Release info

- **Release**: **v86** (era v85, v2.5.0-paolo-ready)
- **Image tag**: `registry.fly.io/fea-pro:deployment-01KSJ2A2Y0683HX5HYJENHPZ36`
- **Image size**: 387 MB
- **App**: `fea-pro`, region `fra`, machine `d8d2949c042208`
- **Strategy**: rolling (single-machine)
- **Tempo deploy wall clock**: ~3 min (build + push + rolling update + health)

## Health check

```
HTTP 200
{"status":"ok","version":"1.0.0"}
```

✅ Backend FastAPI up. `/api/health` risponde.

## Marker versione (HTML bundle production)

```html
<title>FEA Pro — Analisi Strutturale</title>
<!-- v2.6.1 foundation: Plus Jakarta Sans (display) + Inter (body) + JetBrains Mono. -->
<!-- v2.5.0 PR1 destructive fix: aggiunto Inter Tight (display), rimosso IBM Plex Sans (legacy non usato in Precision). Preservato in v2.6.1 (rimozione in Fase 2). -->
```

Conferma:
- ✅ Plus Jakarta Sans font import attivo (Fase 1)
- ✅ Inter Tight preservato come fallback (Fase 2 pianificato)
- ✅ Build serve il bundle nuovo (commit `03d9bd7`)
- `APP_VERSION = "v2.6"` non grep-abile dal HTML perché serializzato nel JS minificato. Verifica visiva: TopBar in pagina deve mostrare "v2.6".

## State macchina

```
PROCESS │ ID             │ VERSION │ REGION │ STATE   │ CHECKS             
app     │ d8d2949c042208 │ 86      │ fra    │ stopped │ 1 total, 1 warning 
```

Note:
- `STATE = stopped` è normale per Fly free-tier auto-stop. La macchina si avvia on-demand al primo HTTP request (cold start ~1-2s, già osservato durante il `curl /api/health`).
- `1 warning` checks: warning su healthcheck path durante cold-start, non critico (la richiesta `/api/health` HTTP 200 lo conferma).

## Rollback (se necessario)

In caso di problema visivo critico bloccante, rollback al deploy v85:

```bash
fly deploy --image registry.fly.io/fea-pro:deployment-01KSHQ1XQ28BG31R32EA47M1EA
```

(Image v85 da memoria deploy v2.5.0-paolo-ready del 2026-05-26 mattina.)

## Cosa verificare visivamente (smoke manuale Federico)

Federico apre https://fea-pro.fly.dev/ e:

1. **Login** con credenziali test (`fede.sanna99@hotmail.it` / `Ciao1234`)
2. **Carica template** "Telaio portale 2D" (o altro)
3. **Verificare shell nuova** (post-load modello, desktop ≥768px):
   - TopBar h-48px con cyan accent + brand "FEA Pro" + tier "FREE" + version "v2.6"
   - Rail sinistra 56px con 5 icone workspace (Box/Cog/Activity/CheckCircle/Shuffle)
   - Viewport con 6 HUD ai 4 corners + center top/bottom
   - RightPanel 380px con tabs Radix
   - StatusBar 28px in basso con counters + "v2.6"
   - ⌘K apre nuova command palette con cmdk
4. **Verificare bug card overlap top-left risolto** (info modello distribuite in TopBar + StatusBar, non più HUD overlay)
5. **Verificare Run button** verde gradient + Esegui statica funziona (preserva logica `useRunAnalysis`)
6. **Verificare Undo/Redo** funzionano (modelStore history preservata)

Se qualche elemento è off rispetto al mockup `Nuovo Guscio.html`, segnalare con screenshot.

## Backward compatibility production

In modalità chrome legacy (Dashboard senza modello, mobile, focus mode):
- Layout legacy mostrato: TopBar (vecchio), LeftRail, MissionBar, ecc.
- Dialog/wizard sempre montati al root indipendentemente dalla shell

Quindi nessuna regressione su:
- Login flow
- Dashboard "modelli recenti"
- Wizard import / template / report
- Mobile responsive

## Quality gate fly post-deploy

| Check | Atteso | Risultato | Status |
|-------|--------|-----------|--------|
| Build remote | OK | OK (image 387 MB) | ✅ |
| Push registry | OK | OK | ✅ |
| Rolling update | OK | OK (1 machine acquired/released) | ✅ |
| Health endpoint | HTTP 200 | HTTP 200 | ✅ |
| Marker bundle HTML | "v2.6" comment | "v2.6.1 foundation" presente | ✅ |
| Release version | v86 | v86 | ✅ |

## Sync repo

- HEAD locale: `03d9bd7`
- origin/test: `03d9bd7` ✅
- origin/main: `03d9bd7` ✅
- tag `v2.6.2-shell`: pushato ✅
