# v2.5.8-cluster-e-information-architecture · Report

**Data**: 2026-05-26
**Branch worktree**: `claude/magical-feynman-792f78`
**SHA HEAD post-T4**: `0b86526`
**Tipo**: feat + fix (information architecture + i18n)
**Stima vs reale**: 2h previste / ~50 min effettive

## Sintesi

Sprint S4 cluster E. **BUG-016+017** chiusi: MakePanel HUB_CARDS sub-text riscritti in italiano user-facing (niente più "Albero", "Wizard discretizzazione (line/quad/h8)", "Fixed/pinned/rollers/springs", "Climate apply"). Card "Import / Export" → "Importa modello" (chiude doppia destinazione I/O; export interamente delegato al rail destro Strumenti).

**BUG-039+045** chiusi: nuovo componente condiviso `components/ui/ModelStatsBadge.tsx` con 2 variant (`compact` 1-line · `detailed` grid 4-col). `MakePanel.CountsRow` (componente interno) eliminato e sostituito con `<ModelStatsBadge variant="detailed" />`. Dedup completato. StatusBar mantenuto separato (format runtime `N: E: DoF: 3D units`, semanticamente distinto da stato build).

**BUG-018** chiuso: 6 modifiche dirette di termini inglesi user-facing. `Wire/Solid/Transp` → `Wireframe/Solido/Trasparente` (3 file). `Engine` → `Motore di rendering`. `Tools/Inspect/View/History` (label rail destro) → `Strumenti/Ispeziona/Vista/Cronologia`. Le `key` interne (workspaceStore) restano come contract. Termini in MakePanel sub-text già coperti da T2.

**STOP rule #1 attivata**: il MakePanel ha pattern hub-first navigation strutturato con sub-view interne (Geometria/Mesh/Carichi/Vincoli/IO). Refactor completo "azioni dirette" richiederebbe rewriting della navigation + adeguamento test. Scope T2 ridotto come da brief: solo rinomina sub-text + chiusura doppia I/O.

## Task completati

| Task | Esito | SHA |
|------|-------|-----|
| T1 audit IA + naming | ✓ | `27bb849` |
| T2 MakePanel rinomina sub-text + chiusura doppia I/O | ✓ scope ridotto (STOP #1) | `b2d96c2` |
| T3 ModelStatsBadge condiviso + 5 test | ✓ | `206a69f` |
| T4 traduzione 6 termini inglesi → italiano | ✓ | `0b86526` |
| T5 commit + report | ✓ | (questo) |

## File toccati

| File | Tipo | Δ |
|------|------|---|
| `frontend/src/components/ui/ModelStatsBadge.tsx` | nuovo | +103 righe (componente + 2 variant + 6 ModelStatKey + label sing/plur italiano) |
| `frontend/src/components/ui/ModelStatsBadge.test.tsx` | nuovo | +95 righe (5 test) |
| `frontend/src/shell/panels/MakePanel.tsx` | modificato | -53 / +34 righe (5 HUB_CARDS sub-text riscritti + `tab === "io"` semplificato a "Importa modello" + `CountsRow` rimosso + uso ModelStatsBadge) |
| `frontend/src/components/panels/ViewportControls.tsx` | modificato | 1 riga (label viewport mode) |
| `frontend/src/components/viewport/ViewportHud.tsx` | modificato | 1 riga (renderLabel) |
| `frontend/src/shell/panels/ViewPanel.tsx` | modificato | 5 righe (label viewport mode + Engine section title + 2 SegmentButton Engine) |
| `frontend/src/components/shell/RightRail.tsx` | modificato | 7 righe (4 RailItem label + description) |
| `frontend/src/shell/panels/ToolsPanel.tsx` | modificato | 1 riga (title) |
| `docs/v2.5.8-cluster-e-audit.md` | nuovo | doc T1 |
| `docs/v2.5.8-cluster-e-information-architecture_report.md` | nuovo | (questo) |

## Quality gate

| Suite | Risultato | Atteso | Esito |
|-------|-----------|--------|-------|
| pytest | **1669 PASS / 1 FAIL / 7 skipped** (108.41s) | 1669 invariato | ✓ |
| vitest | **664 PASS** (23.82s) in 77 file | 659+ atteso, +5 nuovi | ✓ **+5** (5 ModelStatsBadge; nessun test esistente rotto) |
| tsc | **0 errori** | 0 | ✓ |
| build | **OK** in 17.95s | OK, niente warning nuovi | ✓ (chunk-size storico) |

## Comportamento BEFORE/AFTER

### BUG-016 + BUG-017 · MakePanel sub-text jargon dev

**BEFORE** (HUB_CARDS sub-text):
- "Albero · nodi · elementi · materiali" → l'utente strutturista non sa cos'è "Albero" (data structure dev term)
- "Wizard discretizzazione (line/quad/h8)" → jargon (line/quad/h8 sono codici elemento)
- "Nodali · distribuiti · climate apply" → "climate apply" è inglese verbatim
- "Fixed · pinned · rollers · springs" → 4 termini inglesi tecnici
- "Import / Export" card sub "Wizard DXF/IFC/JSON · Tools export" → doppia destinazione, conflittuale con Tools

**AFTER**:
- "Vista gerarchica del modello"
- "Genera mesh strutturate (beam · piastra · solido)"
- "Concentrati · distribuiti · climatici (neve/vento)"
- "Incastri · cerniere · carrelli · molle elastiche"
- "Importa modello" sub "Da file DXF · IFC · JSON FEA Pro" → solo import, no export. Export interamente in Strumenti rail destro.

### BUG-018 · Termini inglesi UI

| Termine BEFORE | Termine AFTER | File |
|-----------------|----------------|------|
| `"Wire"` / `"Solid"` / `"Transp"` | `"Wireframe"` / `"Solido"` / `"Trasparente"` | `ViewportControls.tsx`, `ViewportHud.tsx`, `ViewPanel.tsx` (3 callsite) |
| Section title `"Engine"` | `"Motore di rendering"` | `ViewPanel.tsx` |
| SegmentButton `"Legacy"` / `"Engine"` | `"Classico"` / `"Accelerato GPU"` (con description "stabile" / "WebGL") | `ViewPanel.tsx` |
| RailItem `"Inspect"` | `"Ispeziona"` | `RightRail.tsx` |
| RailItem `"View"` | `"Vista"` | `RightRail.tsx` |
| RailItem `"Tools"` | `"Strumenti"` | `RightRail.tsx` |
| RailItem `"History"` | `"Cronologia"` | `RightRail.tsx` |
| Description rail `"Compare · misure · cost preview · BIM viewer"` | `"Confronta · misure · stima costi · BIM viewer"` | `RightRail.tsx` |
| Description rail `"Snapshot e undo timeline"` | `"Snapshot e timeline annulla/ripeti"` | `RightRail.tsx` |
| PanelChrome title `"Tools"` | `"Strumenti"` | `ToolsPanel.tsx` |

Le `key` interne (`"inspect"`, `"view"`, `"tools"`, `"history"`) restano invariate: sono contract dello `workspaceStore` (`currentRightPanel: "inspect"`), modificarle rompe le persistenze localStorage + i comandi palette. Solo le `label` UI tradotte. "Wireframe" mantenuto come standard CAD italianizzato.

### BUG-039 + BUG-045 · ModelStatsBadge centralizzato

**BEFORE** (CountsRow interno a `MakePanel.tsx:241-257`):
```tsx
function CountsRow({ counts }) {
  return (
    <div className="grid grid-cols-4 gap-1 text-center mb-2">
      {[{label:"Nodi",value:counts.nodes}, {label:"Elem.",value:counts.elements}, ...].map(c => (
        <div className="bg-bg-panel border border-border ...">
          <div>{c.value}</div>
          <div>{c.label}</div>
        </div>
      ))}
    </div>
  );
}
```
- Logica locale, label hardcoded
- Pattern non riusabile da altri pannelli

**AFTER** (`components/ui/ModelStatsBadge.tsx` + usage in `MakePanel`):
```tsx
<ModelStatsBadge
  variant="detailed"
  show={["nodes", "elements", "loads", "constraints"]}
  data-testid="make-stats-badge"
/>
```
- Componente condiviso. Singolare/plurale italiano corretto ("1 nodo" vs "5 nodi"). 2 variant per casi diversi (compact 1-line / detailed grid). Estendibile a `materials` e `sections`.
- `CountsRow` interno rimosso da `MakePanel.tsx`.

**StatusBar mantenuto separato**: format runtime engine specifico (`N: 5 · E: 12 · DoF: 30 · 3D · SI`) semanticamente distinto da contatori di build. Documentato in audit T1 come closure parziale BUG-045 (le 2 viste non sono ridondanti puro, espongono dimensioni diverse del modello).

## Backward compatibility

- **MakePanel API**: invariata. Solo modifiche di label/sub-text. I `key` HUB_CARDS (`geometria`/`mesh`/`carichi`/`vincoli`/`io`) sono invariati → workspaceStore persisted state continua a funzionare.
- **ModelStatsBadge**: nuovo componente, additive. Nessun consumer esistente toccato (CountsRow era privato di MakePanel).
- **i18n**: solo `label` UI tradotte. Le `key` workspaceStore (`"inspect"`, `"view"`, `"tools"`, `"history"`), gli `id` paletteItems (`"wiz-report"`, `"tools-server-export"`, ecc.) e gli `actionKind` restano invariati. Nessun localStorage `feapro-workspace` esistente si rompe.

## STOP rules

- **STOP #1** (T2 MakePanel pattern drastico): **attivata e gestita**. Scope T2 ridotto a rinomina sub-text + chiusura doppia I/O. Documentato in audit T1 + report. Refactor completo "azioni dirette" lasciato a debt sprint dedicato.
- **STOP #2** (`ModelStatsBadge` già esistente): non attivata. Non esisteva (verificato con `find` + `grep`).
- **STOP #3** (>50 termini inglesi): non attivata. Scope T4 limitato a 6 modifiche dirette (Wire/Solid/Transp + Engine + Tools/Inspect/View/History).
- **STOP #4** (termini in contract API): rispettata. `key` workspaceStore, `id` paletteItems, `kind` solver restano invariati.
- **STOP #5** (vitest scende sotto 659): no, sale a 664 (+5).
- **STOP #6** (build cresce >5kB): no, build invariato.

## Finding aperti (out of scope, debt sprint futuro)

1. **MakePanel refactor completo hub→azioni dirette**: la rinomina sub-text è palliativa, il vero fix è eliminare il drill-in hub e mostrare direttamente le azioni cliccabili (Aggiungi nodo, Aggiungi trave, etc.) — ~100-150 righe + adeguamento test. Sprint dedicato.
2. **MakePanel tab interni**: il testo dei tab Carichi/Vincoli/Mesh ha ancora termini come "Climate Loads" (riga 165), descrizioni tecniche dev-oriented. Tradotti solo i sub-text degli hub, non i contenuti interni.
3. **StatusBar dedup con MakePanel**: closure parziale via componente condiviso ma le 2 viste restano semanticamente separate. StatusBar potrebbe usare ModelStatsBadge variant `compact` se accettiamo di perdere il format specifico "N: E: DoF: 3D units". Decisione UX-design da prendere.
4. **TopBar model stats**: non esistono attualmente. Se in futuro si vuole aggiungere un ModelStatsBadge in topbar (alternativa alla bottom status bar), il componente è già pronto via `<ModelStatsBadge variant="compact" />`.
5. **ImportWizard/TemplateGalleryDialog/AICopilotPanel/ComparePanel** continuano con stat inline contestuali. Migrazione a ModelStatsBadge opzionale ma low-value (sono casi specifici, non duplicati ridondanti).
6. **Altri termini inglesi residui**: ~20 callsite minori (es. "DoF" in StatusBar, "BIM" in description) non target T4 perché standard tecnico o acronimo accettato.

## Git history (sprint v2.5.8)

```
0b86526 fix(i18n): Wire/Solid/Transp + Engine + Tools/Inspect/View/History → italiano (v2.5.8 T4, BUG-018)
206a69f feat(ui): ModelStatsBadge condiviso + 5 test (v2.5.8 T3, BUG-039+045)
b2d96c2 feat(panels): MakePanel sub-text italiano + chiude doppia I/O hub (v2.5.8 T2, BUG-016+017)
27bb849 docs(audit): v2.5.8 cluster E information architecture audit (T1)
```

## Sync + tag · raccomandazione

Stesso pattern degli sprint precedenti (worktree feature, push diretto `HEAD:test` + `HEAD:main`, tag `v2.5.8-cluster-e-information-architecture`).

## Prossimo passo raccomandato

Brief **v2.5.10-paolo-readiness-gate** (quality automation finale). Ultimo step della pipeline 5-settimane:
- Playwright E2E flow Paolo (load template → modifica → analisi → verifica → export)
- Rewrite del L2 test falso positivo (`docs/v2_3_4_quality_checkpoint/L2_functional_flows_report.md:51` Ctrl+Z senza mutation reale)
- Smoke live verify sui 3 path export PDF (A button, B palette wiz, C palette quick)
- Verifica visuale BUG-014/015/043/049 fix in v2.5.7
- Verifica MakePanel sub-text italiano (v2.5.8)

Pipeline 5-settimane convergente a ~8 ore reali totali. Velocità ~25× vs piano originale di 200 ore.

9 sprint chiusi (v2.5.1 → v2.5.8). Ready per gate finale Paolo.
