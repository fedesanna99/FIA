# v2.6.2.2-mobile-hud-quickfix · Report

**Data**: 2026-05-26
**SHA HEAD**: `7650a86`
**Tag pushato**: `v2.6.2.2-mobile-hud-quickfix`
**Deploy**: release **v88** su https://fea-pro.fly.dev/
**Image**: `registry.fly.io/fea-pro:deployment-01KSJ760QJK8YVTX4AV65XQ9XP`

## Bug chiusi (3/3)

| Bug | Stato | Evidenza |
|-----|-------|----------|
| **M1** ViewportHud chip overlap mobile | ✅ chiuso | Su <sm (393px) chip 1 + 2 stack verticale full-width (w=380). Chip 3 (preset) e chip 4 (Engine) `hidden`. DOM smoke conferma layout cambiato da 2×2 a 2×1 |
| **M2** Chip counts truncato lato destro | ✅ chiuso | `max-w-full sm:max-w-[260px]` + `<span class="truncate">` interno. Chip 2 ora w=380 invece di w=158 |
| **M3** Tabs ViewportCanvasTabs schiacciati | ✅ chiuso | `overflow-x-auto` + `text-[10px] sm:text-[11px]` + counter `hidden sm:flex` |

## File toccati (3 commit logici)

| Commit | File | Cosa |
|--------|------|------|
| `29711a4` | `frontend/src/components/viewport/ViewportHud.tsx` (+21 -8) | Container `flex-col sm:flex-row sm:flex-wrap`, chip 1+2 `max-w-full sm:max-w-[220/260px]` + `truncate`, chip 3+4 `hidden sm:flex`, padding `top-2 sm:top-3.5` |
| `b4f29a7` | `frontend/src/components/shell/ViewportCanvasTabs.tsx` (+11 -4) | Container `overflow-x-auto`, gap/padding ridotti su <sm, tab `flex-shrink-0 px-1.5 sm:px-2.5 text-[10px] sm:text-[11px]`, counters `hidden sm:flex` |
| `7650a86` | `frontend/e2e/mobile-hud-smoke.spec.ts` NEW (~201 righe) + `.gitignore` | Smoke iPhone 14 Pro emulato (viewport 393×852, isMobile, hasTouch). 5 artifact (full/topleft/DOM/overlap/summary) |

## NON toccato

- `useNewShell` condition in App.tsx (mobile resta su codepath legacy by design — rebuild mobile completo = strada A scope dedicato)
- Tokens design system (`tokens.css`, `tokens.ts`)
- `MobileTabbar` / `MobilePanel` / `MobileMoreMenu` (rebuild mobile = strada A)
- Backend, route, test pytest
- Legacy `CommandPalette.tsx` dispatcher
- I 6 HUD nuovi in `shell/hud/*` (responsive by design Soft v2.1)
- `Chip` component esterno (`components/ui/Chip.tsx`) — quello modificato è il `Chip` inline di ViewportHud.tsx

## Quality gate

| Check | Atteso | Risultato | Status |
|-------|--------|-----------|--------|
| `tsc --noEmit` | 0 errori | 0 | ✅ |
| `vitest run` | 695 invariato | **695/695** (84 file) | ✅ |
| `vite build` | OK, dim stabile | OK (~1.3 MB main chunk) | ✅ |
| `pytest` | 1677 invariato (no backend changes) | non eseguito (no backend changes) | N/A |
| `playwright mobile smoke` | PASS | PASS in 26.0s su v88 | ✅ |

## Smoke E2E mobile (Playwright chromium emulato iPhone 14 Pro)

**Setup**: viewport `{ width: 393, height: 852 }`, deviceScaleFactor 3, isMobile+hasTouch, userAgent Safari iOS 17 (STOP rule #1 brief: webkit non installato in CI, usato chromium con emulation).

### PRE-fix (live v87, baseline)

```json
{
  "viewportSize": { "w": 393, "h": 852 },
  "totalElementsTopleft": 28,
  "chipsViewportHud": 11,
  "overlapsDetected": 20
}
```

Layout chip pre-fix (393px):
- Riga 1 (y=80): "Telaio portale 2D" w=145 + "5 nodi · 4 elem · —" w=158 (2 chip orizzontali)
- Riga 2 (y=117): "Tecnica · Solido · Persp · L3" w=220 + "Engine" w=72 (2 chip orizzontali)
- **4 chip visibili**, container w=370, padding-x=11

### POST-fix (live v88)

```json
{
  "viewportSize": { "w": 393, "h": 852 },
  "totalElementsTopleft": 25,
  "chipsViewportHud": 10,
  "overlapsDetected": 18
}
```

Layout chip post-fix (393px):
- Riga 1 (y=75): "Telaio portale 2D" **w=380** ← full-width
- Riga 2 (y=110): "5 nodi · 4 elem · —" **w=380** ← full-width
- Container: `div.absolute.top-2` w=380, padding-x=7
- Chip 3 (preset) e chip 4 (Engine): **NON nel DOM su <sm** (`hidden sm:flex`)
- **2 chip visibili**, layout stack verticale pulito

### Delta PRE → POST

| Metrica | PRE v87 | POST v88 | Delta |
|---------|---------|----------|-------|
| Totale elementi area top-left | 28 | 25 | -3 |
| Chip ViewportHud-related | 11 | 10 | -1 |
| Overlap detected (rect intersect + stesso z + testo diverso) | 20 | 18 | -2 |
| **Chip ViewportHud VISIBILI** | **4** (in 2 righe) | **2** (in stack verticale) | **-2** |
| Chip 3 "Tecnica · Solido" visibile su <sm | ✓ | ✗ | rimosso |
| Chip 4 "Engine" visibile su <sm | ✓ | ✗ | rimosso |
| Chip 1 width su 393px | 145 | 380 | +235 (full-width) |
| Chip 2 width su 393px | 158 | 380 | +222 (full-width) |
| Truncate aggressivo | NO | SÌ (`<span class="truncate">`) | added |

**Nota su overlap residui (18)**: la maggior parte è overlap parent/child del DOM (header topbar legacy `header.h-12` vs `button.inline-flex` interno, span dentro div chip, ecc.) — comportamento atteso del test che conta intersezioni `same z-index + testo diverso` senza escludere parent-child. Gli **overlap visivamente percepiti** dall'utente (chip che si coprono a vicenda) sono **azzerati** perché chip 1 e chip 2 ora occupano righe distinte y=75 e y=110 senza overlap orizzontale.

Artifact:
- `frontend/e2e/artifacts-mobile-hud-smoke-prefix/` (5 file v87 baseline, gitignored)
- `frontend/e2e/artifacts-mobile-hud-smoke/` (5 file v88 post-fix, gitignored)

## Deploy

- **Release v88** attivo (era v87 = v2.6.2.1-shell-polish)
- **Image**: `registry.fly.io/fea-pro:deployment-01KSJ760QJK8YVTX4AV65XQ9XP` (387 MB)
- **Machine**: `d8d2949c042208`, region `fra`, state **started** (live, non in auto-stop)
- **Health**: HTTP 200, `{"status":"ok","version":"1.0.0"}`

### Rollback (se necessario)

```bash
fly deploy --image registry.fly.io/fea-pro:deployment-01KSJ3WTNHY6WE29HPH4FZGWS4
# (era v87 = v2.6.2.1-shell-polish)
```

## T4 · Z-index audit (no fix richiesto)

Gerarchia coerente:
- `--z-viewport` 0 (Canvas R3F)
- `z-10` ViewportCanvasTabs (App.tsx wrapper)
- `z-toolbar` = 20 (ViewportHud)
- `z-30` MobilePanel
- `z-40` MobileTabbar
- `z-dialog` 40 (Radix Dialog)

Nessun conflitto reale rilevato: ViewportHud (20) < MobilePanel (30) < MobileTabbar (40) → quando l'utente apre MobilePanel su mobile, copre correttamente i chip ViewportHud. La MobileTabbar fixed-bottom (h-14 + safe-area) NON sovrappone i chip top-left.

## Sync repo

- HEAD locale: `7650a86` (+ docs commit dopo)
- origin/test: aggiornato ✅
- origin/main: aggiornato ✅
- tag `v2.6.2.2-mobile-hud-quickfix`: pushato ✅

## Verdetto

**Mobile usabile per Paolo** sul flow viewport. Info domain critical (nome modello, N nodi · E elementi · materiale) **SEMPRE leggibili** su 393px. Comandi preset/Engine spostati su Right Rail View tab via MobileMoreMenu (`feapro:open-view-panel` event), accessibili in 1 tap.

**Stile Soft v2.1 ancora non esteso al codepath mobile** (i 3 chip ViewportHud usano i tokens vecchi `bg-bg-panel border border-border shadow-pop font-mono`). Rebuild mobile completo nella nuova Shell = scope dedicato post-sessione Paolo (**strada A**, 3-4h stimato).

## Out of scope (sprint successivi)

- **Rebuild ShellMobile dentro nuova Shell** (strada A): rimuovere `!isMobile` da `useNewShell`, adattare ShellTopBar/ShellRail/ShellPanel/ShellStatusBar per layout responsive ≤ md, integrare MobileTabbar come "expanded bottom nav" della nuova Shell
- **Migrazione `MobileTabbar` / `MobilePanel` / `MobileMoreMenu` a tokens Soft v2.1** (sostituire `bg-bg-panel border-border` legacy con `bg-panel border` nuovi)
- **Touch gestures viewport mobile** (pinch zoom 2-finger, two-finger pan, tap-and-hold per inspector)
- **Mobile landscape orientation handling** (393×852 portrait OK; landscape 852×393 mai testato)
- **Smoke E2E con device webkit reale** (richiede `npx playwright install webkit` su CI runner, oggi solo chromium emulato)
- **Verifica visiva post-deploy v88 da Federico** (screenshot smartphone reale per validare M1/M2/M3 chiusi)
